# Server acceptance restart, 2026-09-23

The user requested stopping candidate 2738166 and restarting after addressing observed problems. Its million-row G1 run `kit-final-20260923215128-2b601a2a` was interrupted via a verified PID/start-time and pidfd on 2026-09-23 at 23:33:46 UTC. Cleanup passed at 23:34:01; G1 remains FAIL (requested interruption), later gates NOT RUN. Its same-code fast and retained-runtime passes remain separate evidence.

The demonstrated gaps are unavailable operational pipeline reads with repeated PostgreSQL statement timeouts, and a three-hour G1 budget inconsistent with observed 65–80 events/second throughput on the four-vCPU server. CPU quotas are absent for both PostgreSQL and Elasticsearch; observed CPU usage is not an allocation. No resource setting should be changed based on that mistaken interpretation.

Before the next full run, retain a bounded isolated query diagnosis with mixed pending/settled data, plans, and old/new exact-result comparisons. Make only the smallest demonstrated correction; preserve all terminal proofs, unknown states, canonical events, leases, and COMMIT/ACK boundaries. Validate populated upgrade/corruption preservation and current quality. Keep genuine one-million baselines, representative payload and 256-MiB worker caps. Do not substitute synthetic diagnostic rows for acceptance.

Exact final commands in a fresh clean committed server checkout remain `npm ci --no-audit --no-fund`, `npm run tools:provision`, `make verify-functional`, then `make verify`. Do not repeat an unchanged passing fast candidate. The finite G1 budget will be explicitly justified from measured server throughput before execution, with consistent enclosing budgets. All other timeout changes require their own measurements. Retain at least 8 GiB/80 GiB admission and 2 GiB/10 GiB ongoing reserves. Stop tuning after this workload passes; failures require diagnosis rather than automatic identical retries. Open the new run-owned UI through its own loopback SSH tunnel once its gateway exists.

## Selected correction and prospective budgets

The isolated `kit-final-20260923233559-3fe27d3a` query diagnosis is DIAGNOSTIC_RECORDED with cleanup PASS. Original mixed 1M-key snapshot: 22093 ms, repeat 26851 ms; JIT compilation reached 7637 ms in the repeat. The receipt-state component scanned 422233 heap blocks and took 8609 ms. Narrow event staging/delivery-state/consumer-state indexes, grouping delivery states once, and session-local JIT-off for operational pipeline snapshot/backfill reads produced 1536 ms in the selected query plan. The selected query also completed twice under the unchanged 2500-ms statement limit. These copied diagnostic rows are not million-row replication acceptance; cache/autovacuum state differed between measurements. Exact normalized results matched mixed, missing-event, receiver-rejected, missing-observation and real4096-all-settled cases. An initial diagnostic all-settled bulk fixture UPDATE exceeded its 60-second setup limit; its failure is retained separately, not a passing case.

The next 1M G1 drain budget is 21600 seconds (6h): observed server throughput 65-80 events/s implies 3.5-4.3h before margin. Overall fault-child budget is 36000 seconds (10h), its enclosing command 36300 seconds, default full CI wrapper 11h and optional full job 12h. The explicitly selected 2M CLI profile doubles only the drain allowance to12h and overall child to16h; the hosted full job remains the default1M profile. Seed7200, each export3600, each oracle7200, incremental settle360, barriers24, cleanup180, ordinary SQL/lease/remote deadlines and memory/byte budgets stay unchanged. Existing15-second large resource samples retain the64MiB output cap. No CPU quota or worker memory increase is included.

## Subsequent execution scope, 2026-09-24

The server `afee415` attempt failed during a 30-second read-only Compose configuration check; its gates were not run. Measured shared-server quality duration justified the later external Compose 60-second and aggregate-quality 3600-second budgets in `ecdfdb5`, without changing protocol or correctness limits. That server candidate was subsequently interrupted during lint to protect an unrelated production workload. Its old shell wrapper incorrectly recorded exit zero after a signal; the interruption sidecar is authoritative. A corrected external wrapper passed six focused completion/signal cases; the old evidence was not rewritten.

Provider CPU limitation was confirmed and removed with explicit user approval; no reboot or resumed heavy server test followed. Under the user's authorization for independent local checks, the same clean `ecdfdb5` candidate passed local 1024 and default-1M verification. [Final local evidence](evidence/Final-submission-2026-09-24.md) records that result. Server acceptance remains NOT RUN / interrupted, not a server PASS. Earlier failed attempts and the recreated server demo were preserved; this closure does not claim to have repaired that demo's previously failed workers.
