# Server acceptance restart, 2026-09-23

The user requested stopping candidate 2738166 and restarting after addressing observed problems. Its million-row G1 run `kit-final-20260923215128-2b601a2a` was interrupted via a verified PID/start-time and pidfd on 2026-09-23 at 23:33:46 UTC. Cleanup passed at 23:34:01; G1 remains FAIL (requested interruption), later gates NOT RUN. Its same-code fast and retained-runtime passes remain separate evidence.

The demonstrated gaps are unavailable operational pipeline reads with repeated PostgreSQL statement timeouts, and a three-hour G1 budget inconsistent with observed 65–80 events/second throughput on the four-vCPU server. CPU quotas are absent for both PostgreSQL and Elasticsearch; observed CPU usage is not an allocation. No resource setting should be changed based on that mistaken interpretation.

Before the next full run, retain a bounded isolated query diagnosis with mixed pending/settled data, plans, and old/new exact-result comparisons. Make only the smallest demonstrated correction; preserve all terminal proofs, unknown states, canonical events, leases, and COMMIT/ACK boundaries. Validate populated upgrade/corruption preservation and current quality. Keep genuine one-million baselines, representative payload and 256-MiB worker caps. Do not substitute synthetic diagnostic rows for acceptance.

Exact final commands in a fresh clean committed server checkout remain `npm ci --no-audit --no-fund`, `npm run tools:provision`, `make verify-functional`, then `make verify`. Do not repeat an unchanged passing fast candidate. The finite G1 budget will be explicitly justified from measured server throughput before execution, with consistent enclosing budgets. All other timeout changes require their own measurements. Retain at least 8 GiB/80 GiB admission and 2 GiB/10 GiB ongoing reserves. Stop tuning after this workload passes; failures require diagnosis rather than automatic identical retries. Open the new run-owned UI through its own loopback SSH tunnel once its gateway exists.
