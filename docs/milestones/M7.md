# Operator interface: initial scope

Authorized by the user's request to continue and apply all installed interface skills. The skill import has been retained with provenance and licenses. Design: ../ui-design.md. No implementation/browser acceptance is claimed by this initial document.

1. Close M6 execution evidence against the exact tested application SHA; preserve a changed-root identity diagnostic separately from isolated test results. Never rewrite old capture manifests into PASS.
2. Add the Angular operator surface without changing source/event/sink protocols. All nine installed skills inform their own applicable design/review domains.
3. Implement overview, latest/selected backfill, receiver records/details, failures/replay, allowlisted simulations and truthful configuration. Support read-only entry and memory-only explicit operator access.
4. Preserve one in-flight bounded polling cycle, exact numbers, stale/unknown states, keyboard focus and same-key recovery for ambiguous controls. No fake live data.
5. Compile, type/lint/check the interface and exercise the rendered UI at desktop/narrow viewports with isolated HTTP fixtures and clearly separate actual backend evidence. Retain failures and evidence paths.

Required UI cases: U01 unavailable/empty/warming states; U02 fresh/last-known/identity-aware rates; U03 record search pagination/details and exact identifiers; U04 confirmation/auth and same-key retry; U05 backfill accepted/pausing/complete-with-errors distinction; U06 history/quarantine versus replayable failure; U07 explicit simulation confirmations and no startup mutations; U08 responsive keyboard/dialog/focus/reduced-motion/contrast; U09 no live-data fixture fallback or credential persistence; U10 core backend untouched and existing quality checks preserved.

The raw task conversation and internal hiring discussions are not repository documentation. Commit scopes identify actual components. No history rewriting or remote publication is authorized.

## Live observation refinement

The read-only status route delegates to OperationalMonitor, reusing its existing consumer-identity and throughput validation. `test:ui:live` explicitly requires a real rendered observation from the isolated operational fixture; its environment flag does not allow the runner to omit its evidence. This is separate from browser HTTP fixture cases. No new source, sink, consumer or SQL mutation contract is added.
