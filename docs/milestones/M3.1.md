# M3.1 — CI prerequisites and independent degraded-state reconciliation

Authorized 2026-09-19. Actual start: clean `main` at reviewed documentation HEAD `c3cb8f7c8398d8db4d158f2307f790244f4199e6`; no later commits or unrelated edits. M3 application/test baseline is `d576de1addb9da462280779ba925ba4b96fefc85`. This is a narrow continuation of M3, not a delivery redesign or new sink milestone.

## Historical observation and local baseline

Hosted run [35440622786](https://github.com/dojohubco/kill-it-twice/actions/runs/35440622786), job 105890655984, failed at the documentation HEAD. Its downloaded artifact 10583493305 was independently fetched read-only here and matches SHA-256 `d46f3439e19415161db6abc2410ca642ea7fe027796447747adacc665844fc23`. It records 44 passing unit checks, all eight prior profiles (22/36/16/16/19/19/27/23), then the repository vm.max_map_count preflight rejection before ES01–ES14. The actual lower kernel value was not recorded. Elasticsearch was not shown attempting startup. Missing tests.xml/tests.json added ENOENT sanitization diagnostics; owned PostgreSQL cleanup passed. The M3 upgrade profile never ran. This is historical hosted evidence, not reviewer-local service execution or a reproduction of a low limit on this workstation.

The current local Linux host reports vm.max_map_count=1048576. Node 24.19.0, npm 12.0.2, Docker 29.7.2 and Compose 5.5.1 are unchanged. No local sysctl is changed. Targeted baseline logs/results are `artifacts/m3.1/baseline-c3cb8f7/`. Original successful local M3 results remain historical evidence; append the later CI/review facts.

## Counterexample to reproduce before correction

The unchanged oracle uses the latest source row, skips receiver GET for a dead_letter state and removes that entity from expected search IDs. Reproduce an ordinary valid create v1, real source update v2 with loyalty_points="not-a-number", bounded capture and production delivery. Independently retain v1, actual mapper rejection/dead letter, realtime GET and refreshed search. Predicted receiver state is unchanged v1 while v2 is unresolved, and the unchanged oracle rejects the search set. This is code-derived until the real fixture demonstrates it; it is not a production data-loss claim. Preserve the original oracle in an explicitly historical reproduction test with the baseline identity, isolated from accepted fixtures.

## Intended oracle contract

Verifier-owned declarations identify expected rejected events before delivery, including source command UUID, exact revision identity, source-exported payload text and expected mapper error type/field. Never discover these declarations from intents or dead letters. All undeclared rejections fail. Every declared rejection must actually produce the receiver error and matching immutable diagnostic/attempt evidence.

For each entity, distinguish latest source revision, highest independently expected admissible revision, actual receiver state/absence, expected/observed rejections and convergence. After bounded admissible work drains, require exact highest admissible content or absence if none exists. Retained older live documents and tombstones stay in match_all identity expectations. A latest rejected revision is EXPECTED DEGRADED, never full convergence. A later valid command advances normally while the old failure remains. Use independent source SQL, literal envelope/projection fields and lossless values; preserve all history/bytes/hash/ACK/obligation checks. This is a small deterministic workload declaration, not a second mapper or production failure registry.

## Independent required cases

- O01: valid v1 then real rejected v2; exact v1 retained, real error evidence, expected degraded reconciliation.
- O02: rejected initial revision; no admissible predecessor, require absence.
- O03: valid v3 advances; rejected v2 canonical/dead-letter evidence unchanged.
- O04: valid tombstone then rejected restore; retain exact tombstone until higher valid restore.
- O05: unknown dead letter/missing valid effect fails even if a labeled pure or isolated negative control supplies pipeline-owned dead_letter state.
- O06: reject missing/extra/wrong-version/wrong-projection/wrong-tombstone/copied-hash corruption of retained prior state; actual receiver missing/changed-old-document controls are mandatory, pure controls supplement them.
- O07: preserve original ES05 fixed 500/3 expectations and ES14 history/restart checks, with an explicit pre-delivery rejection inventory.
- P01: deterministic below-threshold prerequisite fails with required/observed/platform/phase; no expensive acceptance command or owned service launch is admitted.
- P02: exact threshold and higher values pass unchanged; no sysctl mutation path in local code.
- P03: absent pre-test reports are NOT RUN, primary setup failure retained, available diagnostics sanitized; cleanup remains separately reported.
- P04: missing report after producer attempt remains a required-evidence failure; sanitizer/real cleanup failure stays distinct and no unsanitized upload occurs.

O cases join the explicit required real ES inventory; P cases are pure/harness checks. Every earlier profile and ES01–ES14 remain required, with zero skipped/cancelled/todo/duplicate results. Baseline reproduction is not corrected acceptance. Declarations/assertions are recorded now; executable tests will be implemented and exercised afterward, without retrospective test-first claims.

## Narrow CI and phase changes

Only the workflow on GitHub-provisioned ubuntu-24.04 may record the current vm.max_map_count, use sudo sysctl -w to raise a lower value to 1048576, preserve a higher value, and verify/log the result. Guard against self-hosted execution. Do not persist tuning, run sudo locally, weaken mmap/preflight checks or add bypass flags. Keep pinned full action SHAs and all tool/service/client versions.

Factor a read-only observation/guard, recording observed and required values, platform and phase. The complete gate checks it before prior profiles or services; standalone M3 checks it too. Explicitly assume Linux with the Docker daemon on the same kernel: local /proc does not configure or prove a remote daemon. Local remediation is documentation for an authorized operator, never automatic execution. A controlled reader injection is only a unit-test dependency, not CLI/environment configuration.

Track test-launch/evidence-production phases. Missing reports before their producer is attempted are NOT RUN; missing expected reports afterward invalidate acceptance. Preserve primary, evidence and real cleanup diagnostics separately, sanitize only available data and never fabricate an empty passing report. Update the same verify-m3 entry point/CI selection with early gating and the new inventory; make quality remains local, read-only and service-independent.

## Evidence and exclusions

Use chronological local commits for this scope, actual reproduction, oracle correction, CI/phase reporting and final evidence. Run targeted reproduction/regressions, quality and two fresh complete gates at one committed clean application/test SHA. Run full make verify once expecting nonzero G1–G5. Bundle input hashes, actual receiver versions/rejections, declared expectations, rejected negative controls, faults/outage, failed and successful evidence, full diff/chronology and checksums; distinguish later documentation HEAD. No corrected hosted PASS unless it actually runs; no push/dispatch/publication is authorized.

No production delivery state/migration/architecture change is intended. Preserve file-realm/TLS/restricted grants, canonical bytes, exact external versions, fenced settlements, original faults and actual 60-second outage. No RabbitMQ/M4, consumer, backfill, seed/activation, replay API, UI/API, deployment, scale claim, new dependency or quality tool. Stop after M3.1 for independent review.

References: [Elastic system configuration](https://www.elastic.co/docs/deploy-manage/deploy/self-managed/important-system-configuration) and [GitHub runner.environment context](https://docs.github.com/en/actions/reference/workflows-and-actions/contexts). The project retains its existing 1048576 guard; this is not a claim that every Elasticsearch configuration has that exact minimum bootstrap threshold.

Local targeted baseline completed before this scope commit: `make quality` exit 0 (44 unit checks); `npm run test:integration:m3` exit 0, run `m3-20260919115751934-781a7ce3`, all 14 original ES cases and cleanup PASS. Baseline application files stayed unchanged throughout. These schedules do not include the rejected-update counterexample.
