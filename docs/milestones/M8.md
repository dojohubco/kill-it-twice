# Integrated local runtime and final verification

Authorized continuation: 2026-09-21, starting at clean `1f220df69d9ba2007399585617896f5ff5afaa3e`. This milestone integrates the accepted workers and operator interface. It does not change delivery, canonicalization, checkpoint, acknowledgement or historical-replay invariants.

## Implementation sequence

1. Provide a root Compose application: isolated source/state PostgreSQL services, authenticated TLS Elasticsearch/RabbitMQ, fixed Toxiproxy routes, separate worker processes, operational API and same-origin Angular gateway. Initialization owns administration; workers receive only their own credentials and no Docker socket.
2. Make initialization repeatable under retained volumes, with per-database migration hashes and existing receiver identity checks. Never recreate a previously registered lost receiver or silently rotate credentials. `make seed` resumes the existing bounded baseline recipe, seals/activates it, and starts one durable backfill. Ordinary worker startup never seeds or resets state.
3. Verify cold start, repeated seed, retained restart, actual UI reads and ongoing source/capture/backfill/receiver/consumer convergence. Stop/delete only resources created by the verification invocation.
4. Replace the final verifier placeholder only with independently checked executable gates: G1 actual crash/resume; G2 observed duplicates/reordering and single effects; G3 real ES outage with independent sink progress; G4 one real 500-item bulk with exactly three rejects; G5 useful metrics, diagnostics and rendered controls.
5. Keep small correctness fixtures separate from the planned 2,000,000-row capacity profile. Bound worker memory and exported reconciliation. Record measured results, prerequisites and incomplete work; never extrapolate a small run into a large-data PASS.

## Required evidence

R01: Compose cold initialization has distinct databases/credentials, retained identities and loopback-only public access.
R02: Seed interruption/resume and identical repetition preserve baseline identity/content and never turn baselines into source mutations.
R03: Concurrent backfill and incremental writes converge in both receivers and committed consumer effects.
R04: Retained restart keeps data, credentials, registrations and durable progress; missing registered state fails closed.
R05: Native Angular UI reads real operational state through the normal gateway without fixture fallback.
R06: Runtime memory/process/storage limits and resource ownership are explicit; no privileged host change or unrelated cleanup.

G1-G5 are not aliases for historical milestone test counts. The final oracle must compare identities, versions, actual values and tombstones, plus deduplicated mutation effects. Expected failure exclusions originate in verifier-owned declarations, not production DLQ status. Preserve deliberate oracle corruption controls.

A runtime or individual gate may be implemented and tested before the full capacity profile finishes. Report those scopes separately. The original M6 changed-root capture remains historical FAIL. No push, remote dispatch, deployment, history rewrite or whole-host cleanup is authorized by this continuation.
