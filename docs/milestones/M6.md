# M6: operational backend and controlled replay

Authorized 2026-09-20. Initial checkout is clean main at ab00bdd7409a3d65269a864925878ee5a12deddb. Accepted application/test baseline is 92b1b26bbd7dc63e38fe494bd52653a1263d5724. Hosted run 35508282031 was observed in_progress with no conclusion during initial inspection. Neither that observation nor local workflow validation is hosted acceptance. The unchanged `npm ci --no-audit --no-fund` / `make verify-m5b` baseline is being retained under ignored artifacts/m6; its outcome will be recorded separately.

Implement one Nest control API under apps/control-api, additive operational database functions and ES replay history. Keep the root package, pg, worker scripts, canonical bytes, transaction ownership, source ACK and consumer COMMIT-before-ACK boundaries. ADR 014 freezes the HTTP, metrics, authorization, replay and audit contract before implementation. scripts/required-operational-cases.ts is the independent inventory; IDs do not depend on implementation discovery.

## Required acceptance

| ID   | Independent requirement                                                                                                                                                                       |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OP01 | Actual HTTP and generated OpenAPI paths/methods/statuses; UUID/BIGINT/cursor/limit/body validation; deterministic errors, idempotency and secret exclusion.                                   |
| OP02 | Fresh idle source has zero backlog and null pending age; unreachable is unknown; delayed/leased/blocked and real sealed/unsealed backfill state remain distinct.                              |
| OP03 | Durable backfill start/pause/resume; same key replay; process death after COMMIT before response; completed run cannot restart.                                                               |
| OP04 | Real ES current/tombstone search, bounded server pagination/query, exact string versions, degraded current receiver evidence.                                                                 |
| OP05 | Bounded event metadata/hash/source ACK/sink attempts/consumer observations match direct SQL; malformed and missing identities fail closed.                                                    |
| OP06 | Parse real metrics; exact HELP/TYPE; fixed label sets; durable totals survive process restart; gauges decrease, counters do not; true timestamps and idle/unavailable distinction.            |
| OP07 | Real mapper rejection caused by temporary receiver configuration, controlled repair, SAME immutable event replay, new attempt, satisfaction and unchanged original failure evidence.          |
| OP08 | Unchanged invalid mapped data rejects again; two immutable failure rows/distinct attempts, latest terminal identity, no cross-sink/source reset.                                              |
| OP09 | Replay key repetition, concurrent competing keys, stale terminal attempt refusal, post-COMMIT process death and original-result retry.                                                        |
| OP10 | Source business correction produces a new version; old event/failure/replay remain historical.                                                                                                |
| OP11 | Real consumer poison quarantine is inspectable/non-replayable; no raw-body replay endpoint; consumer DB outage is not poison.                                                                 |
| OP12 | Named deterministic fixture create/update/delete/restore through source command authority; corrupt simulation reaches real ES rejection and normal broker/consumer effect.                    |
| OP13 | Actual configured ES/Rabbit Toxiproxy disconnect/reconnect affects workers; no arbitrary names/URLs, Docker socket or shell execution.                                                        |
| OP14 | Bounded structured outcomes/audit; request correlation; no secrets/bodies/raw quarantine; primary/cleanup error distinction.                                                                  |
| OP15 | Actual API process restart preserves status/metrics/replay and independent worker progress; startup causes no mutations.                                                                      |
| OP16 | Frozen mixed failure fixture pages without duplicate/omitted identities; bounded contexts and histories.                                                                                      |
| OP17 | Populated M5B forward upgrade retains exact source/canonical/run/checkpoint/receiver/consumer and terminal failure identity; terminal backfill phases stay historical.                        |
| OP18 | JSON, metric samples and logs independently locate backfill, useful settlement throughput, incremental pending state, failure counts and unhealthy dependencies. Backend G5 preparation only. |

`make verify-m6` must run the accepted prior bounded profiles once, then fresh operational and populated M5B upgrade profiles. Targeted runs precede two clean committed-code complete gates. Select a finite CI wrapper/workflow deadline using measured gate duration plus headroom. Preserve failure artifacts and input digests; distinguish tested code SHA from later documentation HEAD. Full `make verify` remains honestly incomplete. No UI, frontend files, scale benchmark, final G1-G5, deployment, publication or remote execution. Stop for independent M6 review.
