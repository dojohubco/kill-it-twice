# M5B: concurrent backfill, atomic page progress and finite completion

Authorized 2026-09-20. Inspected clean `main` at reviewed documentation HEAD ab097aa95b961d0b02eee29eda706ba910bd5493, with no later commits or unrelated changes. Reported M5A application/test identity is 4178e2112452881a708cba5e0a5689f57073df96. The reviewer inspected code, not local services or a completed M5A artifact. The read-only observation of hosted run 35498634663 remains in_progress with conclusion null at this writing; it is not PASS.

Read SPEC, AGENTS, accepted ADRs and source/pipeline/receiver boundaries. Node 24.19.0, npm 12.0.2, Docker 29.7.2, Compose 5.5.1 and the existing vm.max_map_count=1048576 are available; no host setting or pin changes. `npm ci --no-audit --no-fund` exited 0. The unchanged `make verify-m5a` baseline is running with scoped resources and exact logs under ignored artifacts/m5b/baseline-ab097aa. This design-only commit changes no baseline executable input; later profile manifests may identify its documentation HEAD. The completed result will be recorded rather than anticipated.

ADR 013 and scripts/required-backfill-cases.ts freeze the proposed implementation and independent behaviors before production changes or execution. This is the planned convergent model, not a newly discovered requirement. Backfill discovers current source revisions through bounded keyset pages. Its finite required set is the union of observed/staged scan revisions and a retained source snapshot of committed mutation identities. Page staging and checkpoint progress share one pipeline transaction. Fence creation is a separate short source transaction; import progress is atomic with pipeline staging. Source ACK remains capture's responsibility. Completion requires both sinks and processed consumer evidence.

The independent BF01–BF17 inventory covers identity/partitioning; rollback and exact checkpoints; real page pre/post-COMMIT kills and healthy controls; concurrent scanner ownership; current-state/capture races; late allocation/commit; delivery lag; pause/resume; exact fence cuts; real fence pre/post-COMMIT kills and controls; import crash/recovery; degraded/error and corruption controls; post-fence activity; independent reconciliation; populated upgrade/restart/permissions; and pre-transfer byte/count limits. The executable inventory selects fresh and populated profiles explicitly. Fixtures use at least 257 genuine seeded baselines, mostly initially unreplicated, actual source commands and real receivers. No test substitutes explicit-ID staging for the automatic scanner.

Only M5B is authorized. Preserve consumer wire limits, command/no-op receipts, isolation guards, sink evidence, canonical bytes and historical tests. No UI, HTTP control plane, public replay, deployment or two-million-row benchmark. Existing seed validation/status scaling observations remain known measurement work; no whole-run recount belongs in a new per-event/page trigger. New commits use subsystem scopes, never milestone scopes. No push, remote dispatch, history rewrite or privileged workstation changes.

## Baseline results before new service execution

`make verify-m5a` exited 0 at 2026-09-20T09:01:58Z after 18 passing profiles and their scoped cleanup. It began at clean ab097aa. The design-only commit 565ee99 and unreferenced M5B files/inventory were developed while it ran; all executed earlier implementation/test files remained unchanged. This is preserved baseline evidence, not final clean M5B acceptance. Exact per-profile input/status records and the outer command log are retained under artifacts/m5b/baseline-ab097aa.

Read-only hosted run 35498634663 subsequently completed success at ab097aa. Artifact 10600659769 has verified SHA-256 9790b3a522952de8d7f129d182ca99b89fe1f4022f399ab89e775396d2986401. Inspection found 18 passing profile manifests with counts 22/36/16/16/19/19/27/23/15/15/6/22/22/1/8/8/18/1. This is later artifact inspection, not a claim that the reviewer ran services or that M5B ran remotely. The new empty legacy-active profile gives BF01's actual U=0 boundary its own executable ID BF01E before execution.
