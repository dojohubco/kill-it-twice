# M3.1 local evidence — 2026-09-19

M3.1 corrects the finite Elasticsearch reconciliation oracle and makes the documented CI prerequisite explicit. It does not change source/capture/staging/delivery semantics. An actual rejected later revision can leave an exact older receiver document or tombstone; the verifier now reports that state as expected degraded, never full source convergence.

## Identity and historical evidence

Start/reviewed documentation HEAD: `c3cb8f7c8398d8db4d158f2307f790244f4199e6`, clean main, no later work or unrelated changes. Original successful M3 application/test SHA: `d576de1addb9da462280779ba925ba4b96fefc85`. M3.1 final tested code: `02bf6e23cb29faf17a3c6e10e93467f17172706c`. A later documentation-only commit records these results; its exact SHA, full chronological history, tracked snapshots and complete baseline diff are in the local bundle's identity/chronology files. No push, publication, remote dispatch, history rewrite or privileged workstation change occurred.

The later hosted [run 35440622786](https://github.com/dojohubco/kill-it-twice/actions/runs/35440622786), job 105890655984, failed at the reviewed documentation HEAD. Downloaded failure artifact 10583493305 independently matches SHA-256 `d46f3439e19415161db6abc2410ca642ea7fe027796447747adacc665844fc23`. Its quality passed 44 unit checks, followed by 22/36/16/16/19/19/27/23 prior-profile passes. The first M3 profile stopped at the repository prerequisite before ES01–ES14; no ES case or M3 upgrade ran. Its diagnostic did not record the lower kernel value, so that value remains unknown. This is not evidence that Elasticsearch attempted startup and failed. Missing tests.xml/tests.json added ENOENT finalization errors; owned PostgreSQL cleanup passed.

This account distinguishes inspection of downloaded hosted evidence from local execution. The reviewer did not run services locally or inspect the successful local M3 raw bundle. The original [M3 report](M3.md) and [development account](M3-development.md) remain intact with a dated later-CI note. Corrected M3.1 hosted CI is **NOT RUN**; actionlint and local gates do not establish hosted success.

## Reproduction and correction

Scope `1680ef3` preceded corrections. The targeted unchanged baseline at c3cb8f7 passed `make quality` (44 unit checks) and `npm run test:integration:m3` (14/14, cleanup PASS); logs are `artifacts/m3.1/baseline-c3cb8f7/`. These original schedules did not contain the rejected-update counterexample.

`npm run test:reproduction:m3-oracle` ran committed clean `94c0d74e1a198c0aa448c66d7c4735316a783680`, exit 0, run `m3-repro-20260919120649054-a34d3231`. Real source create v1 was applied/settled. Real update v2 with loyalty_points="not-a-number" was rejected by Elasticsearch with item 400/document_parsing_exception and retained a dead letter. Independent complete realtime GET was identical before/after at v1; refreshed search retained the entity. The unchanged oracle failed its identity-set assertion because its expected set excluded that entity. The reproduction PASS means that exact historical assertion failure was observed, not healthy acceptance. [Machine reproduction](M3.1-reproduction.json) records identities/input/evidence hashes and cleanup. No production data loss was observed.

The historical function is retained only in the explicit reproduction profile. Healthy reconciliation requires an explicit verifier-owned rejection inventory declared before any delivery attempt, bound to source command UUID/revision and exact PostgreSQL-exported payload text. The declaration supports only the deliberate bad-string fixture; it is not a general mapper. Actual declared rejection must match a real receiver error and the corresponding terminal attempt/dead-letter evidence. Undeclared failures reject reconciliation.

For every entity, the oracle records latest source version, highest independently admissible version, actual receiver version/content or absence, rejected revisions and convergence. It checks the highest admissible document exactly, including an older tombstone, and includes it in the refreshed match_all identity set. A latest rejected source revision remains unresolved even when the retained receiver state is correct. A later valid source command advances normally; previous error/event evidence stays byte-identical. Independent source SQL, literal envelope/projection fields, lossless numeric values, canonical bytes/hash, all historical identities, source ACKs and both sink/consumer relations remain checked. These full-history diagnostic queries are limited to the tiny deterministic fixtures, not a production incremental-reader API.

`npm run test:integration:m31` first passed O01–O06 at clean `d1b3baa` (`m3-oracle-20260919121643399-16f6e04f`, exit 0, cleanup PASS). It used the real source/capture/adapter, not manufactured primary mapper failures. Static-development corrections and their limits are retained in [M3.1 development notes](M3.1-development.md).

## Prerequisite and reporting boundary

The unchanged local kernel value was **1048576** on Linux with the default local Docker context. No sysctl or persistent host file was modified. The read-only complete gate now checks required/observed/platform/phase before any expensive profile. Standalone M3/oracle profiles check before any service launch; the ES service helper also checks before provisioning. The assumption is local Linux sharing its kernel with the Docker daemon: local /proc does not configure or prove a remote daemon. README gives operator remediation requiring that host owner's authorization.

Only the workflow's GitHub-hosted Linux ubuntu-24.04 step may record the current value, transiently raise a lower one with sudo sysctl -w to 1048576, preserve a higher value and verify/log the result. It does not target self-hosted runners, persist /etc/sysctl.conf, disable mmap, add a bypass flag or tune this workstation. The same make verify-m3 command runs after that step. Full action SHAs, Node/npm/TypeScript/pg/ES/Toxiproxy/client pins, file realm, restricted permissions and TLS remain unchanged.

P01/P02 use explicitly synthetic injected observations in the unit harness. Below-threshold execution records the observation and invokes no profile/service executor; at/above values retain their values and admit the fixed command sequence. Unreadable/malformed/unsupported observations fail as unknown, not zero. These synthetic values are not the historical hosted kernel value or an actual sysctl change.

P03/P04 distinguish producer phases. Missing reports before test launch are NOT RUN and the run remains nonzero. Existing setup diagnostics are sanitized. A report missing after its producer was attempted is a required-evidence failure, separately listed from resource/credential cleanup failures; the original primary failure remains. A sanitizer error removes the unsafe file and invalidates acceptance. No empty passing reports are manufactured. Full successful runs still require every original report and cleanup result.

## Environment and preserved inputs

Local runtime: Linux 7.2.5-200.fc44.x86_64, Node 24.19.0, npm 12.0.2, Docker 29.7.2, Compose 5.5.1 and the default local Docker context. PostgreSQL 18.6 reports fsync, synchronous_commit and full_page_writes enabled; pipeline encoding is UTF8. The unchanged pins are:

| Component                               | Exact version / image digest                                                              |
| --------------------------------------- | ----------------------------------------------------------------------------------------- |
| PostgreSQL, both databases              | 18.6-bookworm / `sha256:1c59e2c3c818eaa0f0628f695b36e7c9e362d6b219b36a54a32df645cbd7e1af` |
| Elasticsearch                           | 9.5.4 / `sha256:82ac14f43fe701992e601f4cc81e1c0d7dbc5a2576d8cd736006452925df4026`         |
| Toxiproxy                               | 2.12.0 / `sha256:9378ed52a28bc50edc1350f936f518f31fa95f0d15917d6eb40b8e376d1a214e`        |
| Official ES client / lossless-json / pg | 9.5.1 / 4.3.1 / 8.23.0                                                                    |
| TypeScript                              | 5.9.3                                                                                     |

Input manifest SHA-256: `ea0b433791142883989a3407ef957aaec15f57abbfdd1dc427f5a8a908a8f8c0`. `inputs.json` retains each tracked file's SHA-256. Per-run input identities, service/image identities, prerequisite observations and exact commands are retained alongside each run.

## Final commands and profiles

Both complete gates ran the committed clean code above on fresh, separately owned resources. The local capture is `artifacts/m3.1/review-20260919122841931/`.

| Command                         |         Exit | UTC interval, 2026-09-19  |
| ------------------------------- | -----------: | ------------------------- |
| `npm ci --no-audit --no-fund`   |            0 | 12:28:41.960–12:28:44.684 |
| `make verify-m3`                |            0 | 12:28:44.696–12:46:37.509 |
| `make verify-m3 (fresh repeat)` |            0 | 12:46:37.532–13:05:05.718 |
| `make verify`                   | 2 (expected) | 13:05:05.735–13:05:05.751 |

Each complete gate passed **49 unit checks** and these **214 required integration case executions** (the earlier profiles repeat their established tests; this is not 214 new independent guarantees):

| Profile   | Installation                     | Passed in first / repeat |
| --------- | -------------------------------- | -----------------------: |
| m1        | fresh                            |                  22 / 22 |
| m2a       | fresh                            |                  36 / 36 |
| m2b       | fresh                            |                  16 / 16 |
| m2b       | populated M2A upgrade            |                  16 / 16 |
| m2c       | fresh                            |                  19 / 19 |
| m2c       | populated M2B upgrade            |                  19 / 19 |
| m2c1      | fresh                            |                  27 / 27 |
| m2c1      | populated registered M2C upgrade |                  23 / 23 |
| m3        | fresh                            |                  15 / 15 |
| m3        | populated guarded M2C.1 upgrade  |                  15 / 15 |
| m3-oracle | fresh                            |                    6 / 6 |

Every profile recorded failed/skipped/cancelled/todo = 0, complete required evidence and PASS for owned-resource cleanup. All six receiver-bearing profiles also recorded PASS for Elasticsearch/Toxiproxy cleanup. No command timeout, output overflow or cleanup error occurred. The quality steps include read-only formatting, typed lint, TypeScript, Knip, nonsecret Compose validation, actionlint and unit checks. The final documentation-only edit also passed `make quality` (exit 0, 49 unit checks, 13:06:49.891–13:07:10.512 UTC; `documentation-quality.json`/`.log` in the capture directory). This does not replace the committed-code integration runs.

Each complete gate runs the original earlier profiles once, then the two original ES profiles with O07, then the isolated O01–O06 profile. Prior tests are repetitions of their established scope, not new independent guarantees. Each required case must run and pass with zero skipped/cancelled/todo/duplicates. Full make verify remains nonzero and discloses G1–G5 NOT IMPLEMENTED.

## Receiver and negative-control observations

Both corrected oracle profiles exercise these independent expectations:

| Case | Actual receiver and source result                                                                                                                                                                                                                                                 |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| O01  | Valid v1 is retained exactly after the same entity's v2 receives actual item 400/document_parsing_exception. Source v2 is unresolved; reconciliation is EXPECTED DEGRADED.                                                                                                        |
| O02  | Invalid initial v1 has no admissible predecessor; realtime GET requires absence and search excludes only that absent identity. Actual remote rejection and durable diagnostic are required.                                                                                       |
| O03  | A new valid source v3 becomes receiver v3; the historical v2 canonical event/dead letter remains exact. This entity converges.                                                                                                                                                    |
| O04  | Stored v2 tombstone remains exact after an invalid source restore v3. A valid higher source update v4 advances to a live receiver document without changing v3's error. The corrective command is an update because the successful source restore already made source state live. |
| O05  | Pure unknown-dead-letter, missing-admissible-document and planned-rejection-without-evidence controls reject. No fabricated status is persisted.                                                                                                                                  |
| O06  | The real receiver's retained v1 is deliberately removed, then separately changed with the copied hash; each causes reconciliation failure. Owned fixture restoration recovers the exact v1 content. Additional pure version/field/tombstone/extra-identity controls reject.       |
| O07  | Every fresh/upgrade M3 run retains the actual 500-operation bulk with 497 successes and exactly the three predeclared mapper rejections. Original ES14 content/history/restart assertions remain required.                                                                        |

The source-command IDs, immutable event IDs, exact source-exported payload text and payload hashes are recorded in each pre-delivery declaration. Full actual item errors and terminal attempts/dead letters are retained, rather than treating planned input as proof of failure. The isolated profiles each record four real mapping rejections across O01/O02/O04/O06. The original bulk later repairs one bad source entity through a new valid revision; its old dead letter remains. ES06 has its own explicitly declared rejection, so its broader final reconciliation does not derive an exclusion count from whatever the worker produced.

A supplemental Python integer/Decimal offline recheck reconstructs canonical bytes and SHA-256 directly from the saved source exports, compares exact ACK hashes/relations and retained snapshots, and rechecks the saved actual receiver versions/content/rejections. Its code and results are in the local bundle; this is labeled artifact inspection, not an additional real-service run or production-scale reconciliation.

| Run                             | Installation                    | Actual bulk bytes | Stopped-service interval (ms) | Three actual SIGKILL PIDs |
| ------------------------------- | ------------------------------- | ----------------: | ----------------------------: | ------------------------- |
| `m3-20260919123319955-3f5d2296` | fresh                           |            450814 |                  60000.474317 | 669900, 670201, 670437    |
| `m3-20260919123920057-c656a9d8` | populated guarded M2C.1 upgrade |            450798 |            60000.389486000015 | 702178, 702469, 702767    |
| `m3-20260919125117924-132c1c59` | fresh                           |            450814 |             60000.95381699999 | 780158, 780576, 780783    |
| `m3-20260919125710418-72a24484` | populated guarded M2C.1 upgrade |            450798 |             60000.03698399999 | 817450, 817699, 818014    |

O05 explicitly labels its fabricated status/omission data as pure negative snapshot controls. They are never written to the database or called actual receiver rejections. O06 performs real missing/changed retained-old-document controls on the owned receiver, with privileged external_gte only to restore that corruption fixture; ordinary production delivery remains strict external. Wrong version/field/tombstone, extra identity and copied-hash changed-content controls supplement those real checks. Exact before/after source/canonical/dead-letter/receiver observations and errors are in the per-run sanitized artifacts.

O07 retains the fixed three ES05 bad positions (17/233/499) as declarations before delivery, requires exactly those actual rejection identities, and preserves 497 actual successes. The ES06 deliberate mapper fixture is also explicit. ES14 still reconciles all history and actual projections, rejects missing/extra/corrupt documents and proves retained restart equality. The destructive ES11 identity-replacement fixture remains last; its blocked replacement receiver is not described as a globally healthy final index. Existing real SIGKILL/healthy boundaries, Toxiproxy unknown response, SIGSTOP/SIGCONT overlap and actual 60-second outage remain required.

## Chronology

```text
1680ef3 docs: authorize M3.1 oracle and hosted prerequisite correction
94c0d74 test: preserve and exercise the historical rejected-update oracle counterexample
c1492f4 docs: record the real retained-version oracle counterexample
d1b3baa test: reconcile independently admissible revisions and retained rejected-update state
821f31a fix: provision hosted ES prerequisite and distinguish unrun from missing evidence
02bf6e2 test: capture complete M3.1 gates and preserve historical failed evidence
```

The subsequent documentation commit records completed results; its exact identity is in the bundle. No retrospective test-first sequence is implied: scope and required assertions preceded correction, the historical reproduction ran before the oracle change, and final complete runs followed the committed implementation.

## Scope, preservation and handoff

The only contract changes are the dated SPEC/ADR 010 clarification of expected degraded finite fixtures and the explicit CI/local prerequisite/reporting workflow. No SQL migration, production delivery state or business API was added. Preservation evidence compares 28 runtime/migration/toolchain files byte-for-byte with the baseline and checks all dependency pins independently. All eleven canonical body fields, codec, event identities, source receipts/ACKs and sink lease/settlement semantics are unchanged.

[Compact summary](M3.1-summary.json) references actual command exits, run IDs, required cases, source/receiver versions, declared failures, input hashes, exact signals and cleanup. Full repeated event dumps belong in ignored local artifacts. The local review bundle contains the tested/final tracked snapshots, baseline and reproduction, downloaded historical CI failure, successful and failed development evidence, full diff, exact changed-file list, chronological commits and SHA-256 checksums. Paths identify local materials, not remotely accessible publications. Its filename contains the final documentation HEAD; the handoff supplies the exact path/checksum.

No corrected hosted run was executed. M3.1 provides bounded ES-specific evidence, not receiver HA, immunity to malicious concurrent administrator replacement, unlimited buffering, or full G1–G5. RabbitMQ/M4, consumer, backfill, seed/activation (including the known receipt FK obligation), replay API, public API/UI, deployment and large-data benchmarking remain unimplemented. Stop after M3.1 for independent review.

## Subsequent hosted observation — 2026-09-19, during M4 inspection

Read-only metadata now reports [run 35445223962](https://github.com/dojohubco/kill-it-twice/actions/runs/35445223962) completed successfully at documentation HEAD 47e14920d8fa1b1bab154efe253ba85736b28c6f. Downloaded artifact 10584563994 has SHA-256 958538f84aa588e51662c9e44150cd9c5c05adb90dafe47f1d17430dca4dc2e6; its eleven recorded profile counts are 22/36/16/16/19/19/27/23/15/15/6 with cleanup PASS. This is later metadata/artifact inspection, not reviewer-local execution or a new independent full reconciliation. The original report's NOT RUN statement describes its publication time and is preserved above.
