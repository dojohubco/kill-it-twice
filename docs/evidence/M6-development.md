# M6 development record

The initial checkout was clean ab00bdd. The pre-implementation contract/inventory was committed as ed0eeb1. The prior `make verify-m5b` baseline began before any changes. New unreferenced operational files and exact Nest dependencies were developed while prior profiles ran; existing source, workers, migrations and profile code stayed unchanged during that baseline. This is prior-profile execution evidence, not a clean M6 gate. Exact logs are under artifacts/m6/baseline-install.log and baseline.log.

Development observations so far: TypeScript caught a missing parenthesis, response-schema readonly typing, missing official Express declarations and unsafe inferred receiver fields. Added exact development-only @types/express 5.0.6 because the official platform adapter declarations require it under skipLibCheck=false. Typed lint caught untyped SQL result access and input stringification; those are development checks, not service fault evidence. No assertion or invariant was weakened. npm blocked the transitive @scarf/scarf telemetry postinstall; the application does not require or enable it.

The exact Node container reference was resolved read-only as node:24.19.0-bookworm-slim@sha256:a9f5f7c91a432850b2a8a7797adf5eadb6c733ceed61167806cee7ea7fbc29df. No host tuning or remote publication was performed.

Fresh development run m6-20260920122821849-d5289d7d failed after OP01: awaiting each top-level Node test allowed the root after hook to close shared resources after the first registration. Registering the independent cases synchronously preserves sequential execution and defers cleanup until all registered cases finish. The next run m6-20260920123321659-06f58640 reached source fault injection, then failed recovery because Docker stop/start reassigned the ephemeral published source port. The fixture now rejects actual new PostgreSQL connections with ALLOW_CONNECTIONS=false and restores it through the retained fixture-control session. Unknown-source assertions remain unchanged; this is still a real database outage. Both failed artifacts are retained. API child exit evidence is now captured with known secrets redacted.

Inspection also found lossless ES numeric parser wrappers leaking through ordinary JSON serialization. The API now serializes exact numeric tokens with Node 24 rawJSON; identifiers remain explicit strings. OP04 additionally checks the mapped integer is a JSON number.

Run m6-20260920123607553-7364f852 passed 12/17 fresh cases, including real receiver repair, repeated replay rejection, concurrent replay, four COMMIT/SIGKILL barriers, source correction, quarantine and both proxy outages. The remaining primary errors were fixture-control connection scope (PostgreSQL forbids disabling connections to the session's own database) and test cursor decoding that mistakenly used the general 128-character string bound instead of the contract's 2048-character cursor bound. The source fault control now connects to the same owned service's postgres database. Cursor tests use the already-declared bound; API bounds and assertions were not relaxed. Dependent diagnostic/backfill cases consequently failed in that run and are retained as failures.

Populated upgrade m6-20260920123613680-10fb7726 passed OP17. Fresh m6-20260920123922072-ab613d72 passed all 17 cases. These are development profiles, not the required clean complete gates. Generated controller response schemas now describe stable status, entity, event, failure and control envelopes; real HTTP samples are checked against them. Dependency reachability and data-health dimensions are explicit alongside combined health. No frontend is served.

Final targeted fresh development run m6-20260920124226762-3feb1ddf passed all 17 cases with generated response-schema validation, expected-degraded realtime detail and unavailable receiver readback assertions. make quality passed all checks and 61 unit tests (artifacts/m6/quality-development-4.log). The prior bounded baseline's 21 integration profiles spanned 11:49:12–12:26:42 UTC. The 90-minute finite verify-m6 deadline and 95-minute workflow limit provision over twice this measured baseline plus the approximately two-minute fresh/upgrade profiles; final complete-gate durations will be recorded separately. A Knip-only development failure caught an unused test-helper export, removed without changing behavior.

The first clean capture review-20260920124423051 was intentionally interrupted through the harness's SIGTERM/owned-cleanup boundary to close a startup logging gap: malformed private JSON could otherwise reach Node's raw uncaught-error rendering. The production entrypoint now emits a bounded sanitized startup_failure record; OP01 launches that real entrypoint with a malformed secret-bearing file and verifies exit 1, empty stderr and no secret in stdout. Fresh development run m6-20260920125027816-50b54d49 passed all 17 cases; quality passed 61 unit tests. This interrupted capture is not final acceptance.

The exact-pinned Docker image built successfully. The first boundary smoke attempt raced container startup and got a connection reset; bounded readiness polling now covers reset as well as refused connections. The repeat passed on project m6-control-73bfe60a90, image sha256:d15016583070656bffd4c0cee88a1eea6ab1b574aeb5e0650ce9467e7dfdc4a4, host binding 127.0.0.1:34761, non-root node user, read-only filesystem, dropped capabilities, only the private config mount, 18 generated paths and HTTP 401 without a token. Dependencies were deliberately unconfigured for this boundary-only check. Both projects were removed successfully. Files: artifacts/m6/control-image-build.log, compose-smoke-first-failure.log/json, compose-smoke.log/json, compose-container.log. A narrow Docker build allowlist excludes Git, logs, keys and artifacts. OpenAPI is included in sanitized public test artifacts.

Clean capture review-20260920125246177 at 2a1f319 completed its first full gate successfully: exit 0, 23 profiles, 336 integration cases, 2510.995 seconds. Its second gate failed in the existing M3 fresh profile m3-20260920133902064-51d6786d during Elasticsearch log collection. All 15 required ES cases and resource-removal checks passed; diagnostics failure correctly kept the run/gate FAIL. The old collector asserted code=0 before preserving signal/overflow details, so the original cause cannot be conclusively recovered. The observed code was null and no matching kernel OOM message was found in the relevant interval.

A completed ES log contains individual authentication warning lines of 10261 characters, while Compose tail requests 200 lines per service and the shared command collector allowed only 1048576 bytes. A supplementary real child-process reproduction emitting 200 such-sized lines returned code=null, signal=SIGKILL, outputOverflow=true at the old cap, and code=0 with 2052200 captured bytes at 4194304. This supports output overflow as the likely cause, not a proven reconstruction of the discarded flags. The actual failed artifact remains unchanged. The ES diagnostic collector now retains sanitized stdout/stderr plus explicit code/signal/timeout/overflow/cleanup metadata, with a finite 4 MiB cap and the unchanged finite timeout. Overflow or incomplete diagnostics still fail the gate. No delivery assertions, service fault cases or transactional code were weakened.

M6 capture now runs its two complete gates concurrently in independent clean local checkouts of one committed SHA. Each has its own dependencies, TypeScript outputs and freshly owned service projects. Both children must finish cleanup before a capture failure is reported. The repeat checkout and evidence are retained under the ignored capture directory; its dependency/tool installations are removed after execution. This is repeatability evidence, not a throughput/capacity benchmark. Profile inventories still run exactly once per gate.

The earlier container build exposed the base image's npm 11.17.0 engine warning against the root npm 12.0.2 pin. Docker now provisions exact npm 12.0.2 inside the image. Repeated boundary smoke passed on m6-control-c9d6a34bd2, image sha256:98c095663b079fef4f228f4e50bf6d7a9bd38f1a31dae37e799193e966ba7eaa, loopback port 34872; cleanup passed. Original smoke results remain in compose-smoke-original.json and compose-container-original.log. Supplemental collector evidence is artifacts/m6/log-limit-reproduction.json.

Targeted real M3 run m3-20260920135107394-892b6af7 passed all 15 required cases and cleanup after the collector correction. es-log-command.json records code 0, signal null, timedOut=false, outputOverflow=false, 353179 stdout bytes, zero stderr bytes and no cleanup errors. make quality passed all 61 unit tests (quality-development-6.log). The preceding complete PASS and failed repeat remain preserved; final acceptance is rerun on the new committed harness/container input.

Capture review-20260920135641487 at 9c640eb attempted concurrent independent complete gates. Its repeat failed IC14 in populated M2C.1 run m2c1-20260920140041427-5da2179e: source ACK rejected an expired capture claim (P4002). The guard correctly refused the stale owner; cleanup passed. Concurrent host contention is the suspected trigger, not a proven capacity result. The other gate was stopped through its existing SIGTERM/owned-cleanup boundary. This capture is FAIL and is preserved. The review runner now performs the two independent clean-checkout gates sequentially, avoiding competing heavy acceptance workloads. Existing claim leases, assertions and runtime ownership rules are unchanged.

The interrupted root profile m3-20260920140639977-afbf2c03 retained the explicit SIGTERM primary outcome and passed both PostgreSQL and Elasticsearch owned-resource cleanup with no cleanup errors. The independent unchanged populated M2C.1 profile m2c1-20260920141211258-a53b4a48 then passed all 23 required cases, including IC14, and cleanup (isolation-serial-development.log). Quality also passed all 61 unit tests (quality-development-7.log). Final acceptance proceeds sequentially on a new clean committed input.

Contract inspection during capture review-20260920141302683 at 801a338 identified a missing explicit ES unresolved staging-age field. The previous status supplied per-state oldest delivery timestamps but did not expose age from canonical staged_at. The capture was stopped through the same bounded SIGTERM/cleanup path and is not acceptance. Status now returns es_oldest_unresolved_at plus es_oldest_unresolved_age_seconds, both null when no unresolved ES obligation exists. OP02/OP06 compare the recorded timestamp with direct SQL, derive age from that snapshot's observation time and require null again after settlement; generated OpenAPI describes both fields. This read-only correction does not change delivery state or any earlier assertion.

Fresh operational run m6-20260920142245358-892321d7 passed all 17 cases with the explicit staging-age contract, generated schema validation and all real fault/replay cases; all three service cleanup checks passed without cleanup errors. make quality passed 61 unit tests (quality-development-8.log). The interrupted M3 profile m3-20260920141724153-560ec920 also passed owned-resource cleanup. No transactional or canonical-content behavior changed.
