# M3 development observations

These are local development runs, separate from clean final acceptance. The initial design and independent ES01–ES14 assertions were committed in `6826423` before protocol execution. The executable inventory and harness were written and exercised afterward; they are committed with the regressions. This is not a claim that every executable test was committed before its first development run.

The original `c05f3dc` bounded baseline passed. Its eight prior profiles remain required and unchanged in selection. The original M2C.1 reports and later hosted-run observation remain historical evidence, not M3 CI evidence.

| Local run / command                                                         | Actual result and correction                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Early `npm run test:protocol:es`                                            | Initial HTTP 406 from placing content-type in the wrong official transport argument; corrected request options. Subsequent real strict BIGINT/version and mapping experiments passed. See [protocol findings](M3-protocol.md).                                                                                                                                                                                                                                                  |
| `m3-20260916235455481-2a2ac92a`                                             | Setup failed comparing explicitly written mapping defaults with Elasticsearch's returned representation. Store normal `_source` through explicit settings and compare the stable returned configuration.                                                                                                                                                                                                                                                                        |
| `m3-20260916235949189-8d9afcf3`                                             | The privileged corruption test's physical DELETE then lower external-version restore conflicted with retained delete-version evidence. Corrected only the isolated test repair: equal privileged restoration, or a new real source revision for the higher-state fixture. Production uses only strict `external`, never physical DELETE or `external_gte`.                                                                                                                      |
| `m3-20260917000700394-43972d7c`                                             | ES01–ES09 passed; outage restart failed because Docker reassigned an ephemeral published host port. Reserve a run-owned loopback port and retain it across restart. Later failures in that run were not passes.                                                                                                                                                                                                                                                                 |
| `m3-20260917001450633-4acbabcd`                                             | 13/14 cases passed. ES11 discovered that named `index`/`create` privileges permitted actual mapping changes on the pinned server. Isolated probes confirmed it. Use exact write actions plus concrete-index read/metadata privileges; retain actual denial checks.                                                                                                                                                                                                              |
| `m3-20260917002922010-bc71a4bf`                                             | Developmental 14/14 pass, including the same two follow workers recovering automatically after the actual 60-second outage. Later evidence checks and regressions are additional work, so this does not stand in for final acceptance.                                                                                                                                                                                                                                          |
| `npm run test:protocol:es-permissions`, 2026-09-19                          | Exit 0. Tracked reproducer records both broad named privileges allowing mapping PUT and narrow write-action grants denying it (403), while new strict external versions 1/2/3 all apply. Artifact: `artifacts/m3/m3-permission-probe-1789807101146/permission-probe.json`.                                                                                                                                                                                                      |
| `npm run test:integration:m3 -- --upgrade`, `m3-20260919084030644-a3bf6aec` | 13/14 pass, exit 1, cleanup PASS. ES05 encountered real `P4002` expired capture ownership during fixture staging with the earlier test helper's shortened 1.5-second lease. The attempted ACK rolled back; subsequent work stayed recoverable. Ordinary M3 fixture capture now uses existing production 30-second leases/5-second renewal; original earlier profiles and deliberate short-lease fault tests retain their timings. No production lease or capture bound changed. |

All tabled integration runs were marked developmental with a dirty tracked tree. Some development edits overlapped running processes; HEAD and a start-time input hash alone are not an exact immutable identifier for those executions. Full failed logs and actual observations are preserved in ignored artifacts and included in the local review bundle. They are not published URLs. Final acceptance will use committed code and a clean tree throughout.

Code review also tightened independent item resolution, setup settings verification, target-before-intent renewal lock order, and retained request/attempt correlation. The added regressions require an unrelated confirmed bulk item to settle even if another conflict witness is corrupt, source capture restart after ES settlement, changed projection metadata detection, and independent full-history source/ledger/ACK reconciliation. These are implementation refinements, not invented incidents attributed to the external reviewer.

## Acceptance review refinement — 2026-09-19

The preliminary capture starting at `c014ddd` recorded correct ordinary healthy-child outcomes. Review of the assertions found that a later recovery drain could otherwise conceal an incorrect child outcome. ES07/ES09 and the independent evidence checker now require the exact event/generation/applied/settled result before recovery. ES11 also exercises actual bulk delete/update denial, following the separate successful isolated protocol probe. This strengthens the acceptance oracle; it is not a claim that the recorded children failed.

The same review made target BIGINT counters explicitly decimal strings in status JSON, preserving precision through pg's JSON decoder. A rolled-back privileged operational-counter fixture tests 9007199254740993 and signed BIGINT max without rewriting source revision metadata. The preliminary capture is retained separately and is not the final two-gate evidence. Final acceptance restarts from the next committed clean code identity.

## Earlier-profile preparation timing — 2026-09-19

The clean `bc1b1bc` gate stopped at guarded M2C.1 IC07: 26/27 passed and one failed, exit 1 from the native runner (make exit 2). `artifacts/m2c1/m2c1-20260919091030436-7e602403/` preserves `P4002`, rollback completion and cleanup PASS. The stack is at the initial `await drain()` before the IC07 fault child exists. The ordinary preparation helper still used a 1.5-second test lease, and ACK correctly rejected expiration. No SIGKILL is claimed for that failed case. This is distinct from the earlier M3 bulk-fixture failure.

Ordinary preparation/recovery drains now default to the existing production 30-second lease and 5-second renewal. Explicit test workers/claims/private children retain their original short timings; no prior case, schema/catalog/scope assertion, actual expiry proof, signal or source production code is removed or changed. The subsequent full gates must rerun the actual fault. This is an explicit timing correction following observed evidence, not an ignored failure or shortened inventory.

## Receiver authentication during retained restart — 2026-09-19

The clean `89fbb31` capture at `artifacts/m3/review-20260919091426402/` passed its first complete gate. The repeated gate passed all eight prior profiles, then fresh M3 run `m3-20260919093524670-c22a1f0c` failed: 9/14 passed, five failed, cleanup PASS. ES10 stopped after restart with a blocked target; later cases encountered that block. The log at 09:41:39.471 UTC says the native user store could not retrieve the worker because the security index was unavailable. The original nested JUnit error omitted the deepest diagnosis and the failed profile had no final database snapshot. This limitation is retained; the exact target reason is not reconstructed as if it had been recorded. ES10 now saves bounded primary/target/delivery diagnostics before cleanup if its follow worker fails.

`node scripts/es-auth-restart.ts` then independently reproduced HTTP 401 with the same valid native credential during an actual retained restart, followed by HTTP 200 with unchanged credentials and cluster UUID. Local run `m3-auth-restart-1789811330377` exited 0 and removed its resources. Its first diagnostic serializer incorrectly emitted a cloned numeric error token as `[object Object]`; the raw artifact is preserved as malformed diagnostic output, not accepted machine evidence. The tracked reproducer now writes diagnostic token objects with ordinary JSON serialization (no payload/version processing is involved) and records input hashes. Subsequent reruns supply valid machine evidence.

The narrow deployment correction uses a file-realm runtime user and role, keeping actual invalid-credential blocking, concrete-index privileges and delivery logic unchanged. Setup development exposed an atomic-rename failure on individually mounted credential files (exit 70), missing empty tool input files (78), SELinux relabeling when a one-shot Compose tool inherited the live TLS mounts, and missing reload notification for host-updated bind files. The password tool now runs from the exact ES image with only its own private build mount, no network, explicit memory bounds and run-owned cleanup. Controlled setup writes the read-only runtime configuration and performs an initialization restart before distributing credentials; it does not depend on a file-watcher event or change host SELinux settings. These failed attempts and their actual logs remain in the bundle. No sink result or final gate is inferred from setup experiments.

The corrected file-realm control `m3-auth-restart-1789812087473` exited 0 with cleanup PASS across three retained restarts: no HTTP 401/403, and the same retained cluster identity recovered. Its independent HTTP records also contain seven `_na_` cluster-identity responses during startup. Those records led to the narrow unavailable-identity check before any index request, rather than treating an unknown sentinel as an established replacement UUID. The unchanged replacement test still rejects a different actual index UUID. This is a newly observed local startup behavior, not a claim about the previously inspected hosted artifact.

The fresh native reproduction `m3-auth-restart-1789812413735` also exited 0 with valid JSON, input hashes and cleanup PASS. [Compact authentication evidence](M3-auth-summary.json) records both this reproduction and the file control's exact HTTP counts, timestamps, retained identity and local artifact hashes. The controls are supplemental protocol evidence; they do not replace ES01/ES10/ES11 or increase the required-case count. The added pure unknown-identity regression brings the unit inventory to 44.

The clean `3a1475a` targeted profile `m3-20260919101031792-85d11ef2` exited 1 with 13/14 passed and cleanup PASS. The actual 500/3 bulk, faults, automatic 60000.385747 ms outage recovery and permission/identity controls passed. ES14's post-restart privileged refresh failed with `authentication_processing_error` caused by an unavailable `.security-7` primary: its readiness predicate had checked only the now-independent file-realm runtime. The test now waits for its native setup observer to authenticate as well before refreshing and performing the same exact restart reconciliation. Runtime privileges/behavior and all data assertions stay unchanged. Outage failure diagnostics also retain both individual worker errors while awaiting both workers during cleanup.

The `8f7f075` capture at `artifacts/m3/review-20260919102058789/` passed its first complete gate. Its fresh repeat passed all eight prior profiles, then M3 `m3-20260919104303281-180ff21f` failed ES01 (13/14 passed, cleanup PASS): initial setup's file-realm provisioning returned before the native setup observer was ready for the first privileged protocol-index creation. ES14 and outage recovery passed in that run. Controlled initialization now also waits for its setup observer to authenticate after installing/restarting the file realm. This is the same observed readiness distinction at the initialization boundary, not a new delivery semantic or a claimed failure of numeric version handling. Final evidence must restart both complete gates after this correction.

Both administrative readiness checks also require a successful `/.security/_count` with zero failed shards on the retained single-node fixture. This reads only a count, not user/credential documents. It directly checks native-index readability before privileged operations, in addition to authentication and the actual receiver identity checks. Business-worker credentials receive no additional permission or automatic retry.

The supplemental setup control `m3-setup-control-1789815271740` then passed at committed `d576de1addb9da462280779ba925ba4b96fefc85` (exit 0, cleanup PASS), including native-index readability, file-realm authentication and an immediately usable privileged setup write followed by a restricted runtime read. Its local script, log, result and script SHA-256 `39bf1c68dd168b43d8cc51d165d4f8a09a115943478a8cc460048025d9b608f2` are included in the bundle. This is supplemental setup evidence, not an extra required case.

Final capture `artifacts/m3/review-20260919105629284/` subsequently passed two complete clean gates at that same application/test SHA. Each passed 44 unit checks and all ten required integration profiles; the full verifier still returned expected exit 2. See [final acceptance](M3.md) and its [machine summary](M3-summary.json). Earlier failed or rejected captures remain separate and are not relabeled as final acceptance.
