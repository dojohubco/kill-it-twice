# M4 chronological development observations

These are development observations, not final M4 acceptance. The current baseline is recorded in the milestone scope and ignored artifacts/m4/baseline-47e1492.

## Selected-version protocol probe — 2026-09-19

RabbitMQ 4.3.6-management was actually pulled at sha256:de62d9901fb73aaa8767b9c1e9ea32f2938f69d1b83e430f7c5ca854081d2128. amqplib 2.0.1 was installed exactly; its bundled types avoid another dependency. The root lockfile adds only that package and all preceding pins remain unchanged.

The first isolated startup failed because the attempted management.tcp.port=none value is not an integer. The real server emitted its configuration-transform failure before boot. artifacts/m4/protocol-start.log retains the actual diagnostic. The correction uses only the configured HTTPS listener; the subsequent real probe verifies HTTPS and AMQP/TLS, restricts TLS to 1.2/1.3 and disables the image's unnecessary Prometheus plugin. Erlang distribution remains on the private container network, with no published clustering port. The application endpoints use password authentication over CA-verified TLS, not mutual TLS; verify_none on the server concerns optional client certificates, not client-side server verification.

Run m4-protocol-1789826781732 exited 0 and removed its owned container/volume. Its result.json records RabbitMQ 4.3.6, actual queue arguments, application registration identity, byte-exact high-number payload delivery, positive confirm, mandatory unroutable return, denied publisher queue creation, passive declaration with write/read-only permissions, and metadata inspection using a management user with no AMQP configure/write/read grants. The real highWaterMark=1 write returned false and still received a successful broker confirm. This is a selected-version probe, not a substitute for MQ01–MQ16.

The client forwards documented Node socket options, but its bundled SocketOptions type omits AbortSignal. A narrow typed intersection supplies the public Node signal option without a cast or undocumented driver access. Retirement behavior is still required by the actual fault tests. Automatic amqplib recovery is explicitly false. The source envelope and its arbitrary-precision payload text are untouched.

## Delivery/consumer development — 2026-09-19

These executions used dirty development trees identified by each run's input manifest and worktree patch. They are not final acceptance at the commit named in their manifest.

- m4-20260919141837301-50a9580e failed setup with SQLSTATE 42601 in the new deferred consumer-effect check. Parenthesizing the CASE expression corrected the new migration; prior migrations were untouched.
- m4-20260919142142321-dbfe5074 executed MQ01/MQ02 successfully, then the independent inventory correctly failed for missing MQ03. It was an incomplete implementation, not M4 PASS.
- m4-20260919142632948-1634c7e9 exposed ambiguous verifier SQL (42702): joining a second relation USING(source_epoch,entity_id) after an event-ID join gave two candidate columns. The verifier now uses explicit aliases. Later failures in that run included undrained fixtures from the failed assertion.
- m4-20260919143202371-7aab139d exposed a new consumer shutdown defect: buffered ACKs could remain in a channel buffer when connection.close overtook them. Duplicate effects were still prevented, but healthy completion could leave unwanted redeliveries. The connection lifetime now closes each channel with its ordered RPC before closing the connection, under a disposal deadline. This change was made after the real observation, not as a claimed pre-existing guarantee.
- The same run exposed a fault-harness routing problem: attaching a second Docker network could change the run-owned broker's default route. The test network now has lower gateway priority. m4-20260919143830008-47bd2a76 then passed publisher/consumer crash boundaries, all 25 SIGKILLs of one physical publication, the real lost-confirm proxy schedule, mandatory return and overflow. It still failed: broker stop/start reassigned random published ports, and the management connection observation was premature. Fixed per-run reserved ports preserve the intended endpoint through restart; bounded observation waits for the exact connection incarnation.
- m4-20260919144445146-15018f35 passed the above schedules and concurrency/restart tests but failed the consumer-database recovery control: the proxy upstream used a full container ID that was not a working Docker DNS alias. It now uses the actual run-owned attached-network address. The independent oracle also compared PostgreSQL locale order with JavaScript lexical order; sorting both identity sets fixes the comparison without changing membership. Cascading leftover-work assertions remain recorded as failures.

The quarantine identity retains only relevant protocol/identity properties, excluding changing broker redelivery counters and attempt correlations. Exact raw bytes remain durable. Real repeated quarantine redelivery must prove one retained diagnostic. None of these development runs replaced required faults with simulated exceptions.

- m4-20260919144845039-f207064d ran all 22 case bodies: 21 passed; MQ14 failed because the new verifier tried an explicit ES refresh with the restricted runtime credential. The credential correctly denied indices:admin/refresh. The test now uses the existing setup credential only for that explicit verifier refresh; runtime permissions are unchanged.
- The expanded permission/poison test edit initially had a test-local variable shadowing error and a canonicalEvent temporal-dead-zone error. TypeScript/typed ESLint rejected them; aliases and the already-imported protocol function corrected the tests. These are development test defects, not production delivery loss.

Some developmental runs overlapped further test-only edits; they are not immutable reproducible acceptance snapshots. Final gates must use committed, unchanged code. Their raw manifests and failure outputs are retained, not relabeled as passing runs.

- m4-20260919145735053-35711f58 passed all 22 executable cases (zero failures/skips/cancellations/todos), but the overall run failed its evidence finalizer: it requested a single MQ09 record while the actual test deliberately records MQ09-publisher and MQ09-consumer separately. The checker now independently requires both records, plus the production adapter's mandatory-return observation. Cleanup passed. This failed run is preserved and is not final acceptance.

- The populated run m4-20260919150105828-f45f38b3 also passed all 22 tests and failed the already-corrected MQ09 finalizer in its earlier loaded code. Both original manifests stay FAIL. Applying the corrected checker to the first recorded development evidence found six fault schedules, six healthy controls, all 25 distinct crash observations and 120 declared mutations; this parser check does not relabel that run.
- Review of the new AMQP classifier produced a pure counterexample: ECONNREFUSED at port 40321 and a generic handshake termination were classified as auth. artifacts/m4/classifier-before.json records the actual function result. The matcher now requires explicit reply-code/token boundaries; generic connection failures remain transient. This was not a reproduced server outage. A real bad-credential regression separately checks actual auth blocking without per-event dead letters.
- The outage test now also exercises automatic reconnect/retry in the same publisher and consumer follow objects. New broker metadata observations accept an AbortSignal so test deadlines cancel their actual HTTPS requests. This preserves bounded disposal instead of leaving a Promise.race loser running.

## Final coverage review — 2026-09-19

The clean targeted run m4-20260919151130327-7674e8e6 at 0a12d1357f604f67fe14dd8ec6ca32985586e443 passed all 22 cases, retained restart and cleanup, reconciling 121 declared mutations/effects. This is a targeted pass, not a complete final gate.

A subsequent read-through identified a coverage distinction: MQ09 paused an already-confirmed publication result before local settlement, but did not also hold the actual AMQP confirm callback in flight through lease expiry. No production failure was observed. Capture review-20260919151539800 was deliberately interrupted through scripts/m1.ts SIGTERM handling during M3 upgrade setup. Its eight earlier profiles and fresh M3 profile passed; the interrupted upgrade records NOT RUN receiver cases, a primary interruption and successful owned cleanup. The capture remains FAIL with make exit 2 and is not final acceptance.

The added MQ09 schedule uses actual Toxiproxy downstream latency after a real exchange check. An independent consumer observes the accepted bytes while the callback is withheld; database clock proves the 900 ms claim expired, a second publisher settles generation 2, and the old positive callback subsequently arrives. Both the worker and direct guarded SQL must leave generation 2 unchanged. The earlier delayed-settlement and retired consumer-channel controls remain required. This strengthens the pre-existing MQ09 requirement without changing production behavior or claiming a new failure discovery.

The strengthened developmental run m4-20260919153310739-a4250655 then exposed a real bounded-close defect. Under downstream latency, the newer publisher settled generation 2, and the broker independently reported no remaining connections, but the old worker remained pending. Its owned native test group was deliberately stopped after retaining artifacts/m4/inflight-hang-observation.json; this diagnostic SIGKILL is not a required acceptance fault or PASS. Inspection of pinned amqplib 2.0.1 found channel.close's continuation can remain unresolved when the socket ends before CloseOk. Aborting the socket alone therefore did not bound the awaited Promise. AmqpSession now retains ordered healthy channel/connection close but separately bounds that wait at one second, aborts its owned socket, and consumes either late completion or rejection. No dependency, public API, delivery state or transaction semantics changed. The new real schedule must demonstrate timely completion and unchanged generation-2 evidence after this correction.

The corrected developmental profile m4-20260919154348150-c7c92906 passed all 22 cases, retained restarts and cleanup. MQ09 recorded the actual positive generation-1 callback 5001.987 ms after generation 2 settled; the old worker reported confirmed/stale, guarded SQL also returned stale, terminal evidence was unchanged and one consumer effect remained. This duration identifies the controlled fault schedule, not throughput. The corrected quality gate passed 53 unit tests with zero failures/skips. Final complete gates follow the next commit and must remain clean.

Clean capture review-20260919154912451 at e507e6a8d3a00f0d66037f90f1d9e6bd0145bbd6 passed all eleven earlier profiles but failed fresh M4 (16 passed, 6 failed). MQ10 did not observe confirmed automatic recovery within its 15-second bound; later failures encountered undrained fixtures, while final cleanup passed. Retained attempt evidence for that event records transient generation 1 at 16:10:18.226014 UTC and confirmed generation 2 at 16:10:50.244231 UTC. The cause was not yet established by that result alone. Inspection identified a scheduling hypothesis: target and item retry jitter differ, and rabbit_claim allocated a 30-second recovery probe before determining whether any item was due. A supplementary real-SQL control now makes that ordering deterministic before a correction is applied. The follow-test stop condition is also tightened to confirmed settlement; a recorded transient disposition is not recovery. Recovery attempts are recorded before the success assertion so failures retain their diagnostic observations.

Run m4-20260919161502410-7749e259 reproduced the scheduling hypothesis using real restricted SQL calls before production correction: claimed=[], mode=cooldown, probe_owner non-null, probe_generation=2, observed_at 16:18:04.472200 UTC and probe_until 16:18:34.458778 UTC, while the event remained retry_wait at claim generation 1. MQ10 failed the required no-empty-probe assertion. Migration pipeline/005-rabbit-probe.sql now replaces only rabbit_claim: the target row still fences admission, but probe ownership is allocated inside the successfully selected eligible-row path. No rows, identities or terminal evidence are rewritten. The original migration 004 is preserved. Actual broker outage/recovery and the supplemental empty-probe control are both required; the latter is never presented as a remote failure.

After migration 005, developmental fresh m4-20260919162041668-ead919ca and populated-upgrade m4-20260919162446029-b45cd3d2 both passed all 22 cases, retained restarts and cleanup. Their independent reconciliations found 123 and 125 declared mutation effects respectively. Both recorded empty claims with null probe ownership and actual same-worker confirmed broker recovery. make quality passed 53 unit checks with zero failures/skips. These targeted results precede the next committed final candidate; complete final gates are still separately required.
