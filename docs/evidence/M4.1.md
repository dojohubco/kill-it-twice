# M4.1 evidence: consumer wire-byte accounting

M4.1 only. The external review's 93-versus-108 calculation was reproduced against real source-derived revisions, PostgreSQL and RabbitMQ before production correction. No message loss was observed: the rejected cohort rolled back, received no ACK, remained in the queue and redelivered. The forward correction counts exact canonical wire bytes without changing the 32-message/1048576-byte contract.

## Identities and chronology

Original baseline: 77a13205a99f23cd73e353b87bed007f0a36cc75. Reported M4 tested application commit: c109b655169e5ff836df3674d4b681fb75c60928. Final M4.1 tested code: bba64653878ac1dadc7a7fe1ea5d2e4ea9c3860e, on main with a clean tracked tree for both complete captures. The later documentation HEAD is recorded in the local bundle identity.json and final handoff; ignored generated artifacts do not dirty the tested tree.

- 333c7ab: scope and independent B01–B08/B06H inventory, before execution/correction.
- efc1514: real source-cohort, private observation/crash harness and required regression code; production consumer/accounting unchanged.
- 8b231a2: permit the new isolated project prefixes and qualify independent oracle joins.
- 6fc0aad: commit actual old-SQL/real-consumer reproduction evidence before correction.
- 0a375fd: additive consumer migration 002, controlled application and byte-contract clarification.
- 1fbb6cd: scope private cohort timing away from the publisher after the continuation fixture failed.
- bba6465: require all profiles in local CI selection and extend reproducible evidence capture.

The required inventory/design preceded execution. Some corrected-case scaffolding was committed with the reproduction harness before migration 002 existed; it was not executed as corrected acceptance until the migration was implemented. No history was rewritten.

## Minimum correction and byte definition

[migrations/consumer/002-batch-byte-accounting.sql](../../migrations/consumer/002-batch-byte-accounting.sql) replaces consumer.process_batch using the original implementation except its accounting expression. It computes the octet length of UTF-8 `{"body":` + canonical body bytea + `,"content_sha256":"` + hash + `"}`. A valid hash makes the frozen wrapper exactly 93 bytes. Migration 001, all src/ runtime TypeScript, canonical eleven-field content/codec, source/capture/pipeline migrations, lock/receipt/effect semantics and dependency pins remain unchanged. The function retains its OID, owner, SECURITY DEFINER, fixed search_path and privileges; PUBLIC remains revoked.

The 1 MiB limit measures original received canonical wire-body buffers, including duplicate deliveries. It does not measure all JS heap, metadata or hex SQL parameters. Unique-event SQL work may shrink after deduplication but cannot exceed admitted raw-message bytes. Existing 64-KiB canonical-record, 128-KiB broker/quarantine, 64-message prefetch and bounded metadata limits remain separate. No ceiling increase, arbitrary-error splitter, quarantine reclassification or ACK-before-COMMIT path was introduced. SPEC/ADR 011 explicitly clarify this existing contract and demonstrated correction; there is no new architecture or SPEC version.

## Reproduction and corrected observations

[Committed reproduction summary](M4.1-reproduction.json) records clean 8b231a2 and actual backend/transaction/channel/process identities. Thirty-two distinct v2 wires from normal commands, unchanged bounded capture and immutable ledger exports each measured 32768 bytes; bodies were 32675. SQL 001 charged 1049056 despite actual total 1048576. Direct consumer_runtime SQL raised P6002 at the guard and rolled back. Two production Consumer processes submitted that entire cohort and also rolled back with P6002, exited 1, emitted no ordinary success, made zero ACK calls and left 32 ready/zero unacknowledged messages. The second attempt observed all redelivered. Exact existing v1 inbox/effects/projections/totals and quarantine were unchanged. The diagnostic profile PASS means reproduction assertions passed, not successful v2 processing.

Private instrumentation delays the real consume registration's return until actual callbacks have queued the intended cohort. It never changes selection, wire content, SQL results or production transaction ownership. Parent observers see the real idle transaction/backend xid, absent then committed rows and original-channel ACK calls. SQL accounting is independently reconstructed from exported bytes; business effects are compared with source commands/outbox content, not just counters or a worker hash.

- B02: one actual SQL transaction at exactly 32 distinct/1048576 wire bytes commits all expected content/effects and individually ACKs all deliveries. Inbox xmin values match the independently observed transaction.
- B03/B04: 1048577 original bytes process as 31 fitting messages then one retained redelivery; direct over-byte and >32-item inputs raise P6002 for their actual guard. The application also partitions 33 small messages into 32+1.
- B05: real PostgreSQL checks below/at/above, varied lengths, multibyte Unicode, quotes/backslashes, long BIGINT strings and precise numeric payload text. Explicit synthetic baseline representation fixtures are deliberately rolled back, never claimed as source mutations. Sixteen actual source revisions delivered twice charge 32 original wires/1048576 bytes but only 16 unique SQL events/524288 bytes, with one effect each.
- B06/B06H: the actual boundary batch commits before a real SIGKILL without any ACK/ordinary success; a new consumer redelivers and returns already_processed with unchanged effects/totals. Healthy release exits 0 with exactly one ordinary result and 32 individual ACKs. Every run records its own signal/process/channel identity.
- B07: a populated M4 consumer includes ordinary effects, a zero-unit synthetic baseline and real quarantine. Exact consumer/source/pipeline snapshots match across 002; receiver identities and prior broker/ES dispositions remain unchanged. Four actual runtime bypass/replacement attempts are denied. Fresh installation also applies the forward migration.
- B08: the same Consumer object processes the boundary batch and then three new small source revisions without an external reset, spurious quarantine or retry loop; actual publication outcomes are confirmed independently.

## Commands and evidence

The unchanged baseline `npm ci --no-audit --no-fund` and `make verify-m4` exited 0 at 77a1320. Development `make quality` exited 0, followed by clean corrected fresh and populated-upgrade runs at bba6465, eight cases each. The final capture command was `npm run review:m41 -- capture`, with local directory `artifacts/m41/review-20260919183924212`.

| Final command                     | Exit | UTC start                | UTC finish               |
| --------------------------------- | ---: | ------------------------ | ------------------------ |
| `npm ci --no-audit --no-fund`     |    0 | 2026-09-19T18:39:24.295Z | 2026-09-19T18:39:30.485Z |
| `make verify-m4-1`                |    0 | 2026-09-19T18:39:30.499Z | 2026-09-19T19:14:47.234Z |
| `make verify-m4-1` (fresh repeat) |    0 | 2026-09-19T19:14:47.368Z | 2026-09-19T19:47:55.071Z |
| `make verify`                     |    2 | 2026-09-19T19:47:55.095Z | 2026-09-19T19:47:55.110Z |

Each complete gate passed 53 unit checks and 16 profiles with counts **22/36/16/16/19/19/27/23/15/15/6/22/22/1/8/8**, totaling 275 required case executions (including one explicitly historical diagnostic, not application success). Both gates have zero failed/skipped/cancelled/todo required cases. Every run records clean bba6465, distinct resources, retained restart evidence and cleanup PASS; all primary/cleanup error lists in final acceptance are empty. Historical reproduction processes deliberately exit 1 with P6002 inside their passing diagnostic case. Full `make verify` exits 2 and prints G1–G5 NOT IMPLEMENTED.

Input inventory SHA-256: `c43c3167f8a63178b058f407b35b70157b98e4be5cb3cf8faf9824cfc00a6180`. Per-file hashes, exact commands/exits, profile required IDs, manifest hashes, case observations and local paths are in [M4.1-summary.json](M4.1-summary.json) and capture inputs.json/run manifests. New final boundary SIGKILL process identities are 2604990 and 2634506 in the first gate, 2832381 and 2854143 in the repeat; every exit is actual SIGKILL with no ordinary success before kill. Corresponding backend xids, channel IDs, events, wire hashes, committed rows and redeliveries are in each profile's sql-evidence.jsonl. They are not signals borrowed from a development run.

Environment remained Node 24.19.0, npm 12.0.2, TypeScript 5.9.3, pg 8.23.0, amqplib 2.0.1, Docker 29.7.2, Compose 5.5.1, PostgreSQL 18.6, Elasticsearch 9.5.4/client 9.5.1 and Toxiproxy 2.12.0. Local vm.max_map_count was already 1048576; no host tuning occurred. Immutable image pins remain PostgreSQL `sha256:1c59e2c3c818eaa0f0628f695b36e7c9e362d6b219b36a54a32df645cbd7e1af`, Elasticsearch `sha256:82ac14f43fe701992e601f4cc81e1c0d7dbc5a2576d8cd736006452925df4026`, Toxiproxy `sha256:9378ed52a28bc50edc1350f936f518f31fa95f0d15917d6eb40b8e376d1a214e`, RabbitMQ `sha256:de62d9901fb73aaa8767b9c1e9ea32f2938f69d1b83e430f7c5ca854081d2128`. Actual container/image identities and PG durability/UTF-8 settings are recorded per run.

Static checks remain read-only and service independent. All required earlier profiles run once per complete gate; repeating them is repeated evidence of their existing scope, not additional independent guarantees. Full verify remains nonzero because backfill/UI/final G1–G5 integration is unfinished.

[Development notes](M4.1-development.md) retain real setup and test-instrumentation failures. The first reproduction stopped before tests because the service-name guard lacked m41; cleanup passed. The first corrected fresh run passed seven cases but its continuation instrumentation interfered with a publisher channel and timed out. That test hook was scoped correctly and the confirmed-publication assertion strengthened. These are not fabricated production data-loss or broker-rejection findings. No failed assertion was deleted.

## Hosted evidence, local bundle and stop

The initially pending M4 hosted run 35458462908 actually completed successfully at 77a1320. Downloaded artifact 10589247521 SHA-256: 8e2f5081d1c3b8fa4afe6e37215df88160dff5ccb279550c5f7376e27a3e4d57. This agent inspected its 13 old profile manifests/cleanup; the reviewer did not execute local services or inspect that completed artifact during the earlier review. The later observation is appended to historical M4.md. It does not cover M4.1. Hosted CI for this correction is NOT RUN; actionlint/local gates do not constitute hosted acceptance. Nothing was pushed or dispatched.

Reproduce packaging with `npm run review:m41 -- bundle artifacts/m41/review-20260919183924212` after committing this report/summary. Local review directory: `/home/tornike/Documents/Projects/kill-it-twice/artifacts/m41/review-20260919183924212/`. Its `bundle/identity.json` records the tested and final documentation SHAs; `bundle/full.diff`, `bundle/changed-files.txt` and `bundle/chronology.txt` provide the complete reviewable change and chronological commits. `bundle/SHA256SUMS` verifies every included file; the generated `m4.1-review-<documentation-head>.tar.gz.sha256` verifies the archive. The final handoff names the actual archive and documentation SHA after packaging.

The bundle contains tested/final tracked snapshots, full diff, changed-file list, chronological commit metadata, input hashes, both complete captures, earlier baseline, real historical reproduction, failed development evidence, sanitized logs and SHA256SUMS. Paths are local, not published download locations. Certificates/credentials remain excluded. Both database services and the broker/ES resources are cleaned only by their owning run.

Stop after M4.1. No baseline activation/no-op FK correction, seeding/backfill/checkpoints/fences, replay API, UI, deployment, large benchmark or new runtime architecture. Existing single-node/trusted-runtime assumptions remain; no distributed exactly-once or full G1–G5 claim is made.
