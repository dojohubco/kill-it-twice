# M5B development observations

These are development results, distinct from final clean acceptance and from reviewer evidence. The planned convergent model was supplied before this work; implementation corrections are not invented architectural discoveries.

- Starting baseline: npm ci exited 0 and unchanged make verify-m5a exited 0, 18 profiles, 2026-09-20T08:24:02Z–09:01:58Z. Design-only commit and unreferenced new M5B files were created while it ran; prior executed implementation/test files were unchanged. Preserve the per-profile inputs/status patches under artifacts/m5b/baseline-ab097aa.
- Static development checks caught erasableSyntaxOnly parameter-property syntax and an unknown thrown renewal value. Those were corrected with explicit fields and narrowing; no compiler/lint rule was disabled. Knip identified three internal-only helper exports; export visibility was removed rather than suppressed.
- First real-service attempt at clean 0b3f5b5: npm run test:integration:m5b exited nonzero, run m5b-20260920090533107-17d8c2f7. The existing ES harness project-name allowlist rejected the new m5b prefix before ES startup/test launch. All required M5B cases were NOT RUN, not failed assertions or PASS. Run-owned PostgreSQL cleanup passed with no secondary errors. The bounded project allowlists were extended explicitly for the new profile; no host setting or service pin changed.
- Second attempt at clean facff50: run m5b-20260920090643482-e6b7062f failed before tests when PostgreSQL rejected pipeline migration 006 with SQLSTATE 42601. An isolated pinned-PostgreSQL compiler probe located position 19548, an unparenthesized CASE expression in a PL/pgSQL IF condition. Parenthesizing that expression allowed all six pipeline migrations to commit in the probe. The probe is migration compilation evidence, not receiver acceptance. Both the failed profile and probe cleaned their owned resources; full logs are retained under artifacts/m5b.
- First behavioral run, m5b-20260920091414887-a55f12fb, reached the real source/receiver fixture. Page staging rejected SQLSTATE 42702 because the PL/pgSQL loop variable and a JSON table alias both used x. Explicit distinct aliases correct this. The callback-rollback test also used an incorrect message regex; it now requires the original deliberate callback error, rolled_back classification and no SQLSTATE, so an unrelated SQL failure cannot satisfy it. Session observations now match the exact child name or its colon-delimited operation suffix, excluding similarly prefixed observer names. This shared-fixture run failed overall with downstream failures; none is accepted evidence. Cleanup passed. The separate upgrade case now explicitly restarts both real receivers and compares their identities/documents, in addition to the enclosing database restart checks.
- Run m5b-20260920091838604-c4e7e578 passed 21/22 cases and failed BF07: the test's module-level teardown was registered before sequential top-level awaited tests, allowing it to close the deliberately held cross-case source session early. The real consumer/pipeline behavior was not the cause. Teardown registration now follows the sequential tests. No assertion was removed. The next revision adds observed concurrent source-fence key arbitration, exact primary-error checks for import rollback and a real self-consistent canonical-content conflict inside the page SQL boundary. Development edits made while this run was active are not identified solely by its HEAD; its input manifest and diff remain retained.
- At f15079c, fresh m5b-20260920092303480-59576dd9 passed all 22 cases and cleanup; populated upgrade m5b-20260920092606644-5bb4c093 passed BF16, including real retained receiver restarts. Quality passed all subcommands and 57 units. The final audit then tightened committed-batch replay to reject missing run membership (with a rolled-back privileged corruption control), and expanded the separately inventoried legacy-active case to cover a populated epoch without a bootstrap key after its empty U=0 run. These checks implement the existing contract; they are not a new discovery or a scale claim. Final complete gates will use the later committed state.
- The revised legacy-active profile passed at 9670847 (m5b-empty-20260920092935572-1b0cfd66). The fresh replay-guard target passed 22/22 (m5b-20260920093131150-f9034862; development tree with inventory-comment changes). Cleanup passed for both. A test audit then isolated the quarantine classification control from the unrelated real ES dead letter: inside a rolled-back privileged fixture, the previously healthy run now has zero ES errors and exactly one quarantined observation, and must classify complete_with_errors. This is explicitly local classification evidence, not a manufactured receiver quarantine; the prior real consumer poison/crash tests remain required. Original inventory timing is documented precisely: BF01E splits an existing BF01 requirement and was added before that separate profile ran.
- At c0636e9, fresh m5b-20260920093532348-1c3836ed passed 22/22 and cleanup; quality passed with 57 unit checks. During the first complete capture, a test inspection found BF09 recreated a worker object but did not explicitly prove pause/resume through a replacement scanner process. The capture was deliberately stopped after the active M4 profile finished and cleaned up: review-20260920093859233 retains 12 passing profiles, followed by an operator-initiated SIGTERM of the owned make process. It is incomplete/nonzero acceptance, not an application fault or a required SIGKILL test. BF09 now launches distinct real processes while paused and after resume, checks unchanged paused state, retained checkpoint/bounds, fresh incarnation IDs and session cleanup. Final complete gates use the strengthened committed test.
- A supplemental independent Python snapshot audit initially assumed the consumer projection duplicated inbox body bytes and raised KeyError. The corrected audit follows the projection's retained inbox identity. This was an audit-script assumption, not a production defect. Both diagnostics are retained; the corrected development audit independently checked canonical bytes/hashes, source history/ACKs, inbox/effects/aggregates, obligations and scan/fence membership without importing production code. It does not replace real-service acceptance or establish scale.
- The strengthened BF09 target passed 22/22 at clean 92b1b26 (m5b-20260920100615948-fd7327cf), including three actual process incarnations, preserved pause state and resumed cursor 112 -> 128. Final capture review-20260920100913274 then passed two full clean gates at that commit, all 42 profiles and their cleanup, with expected full-verify exit 2. Independent audits of both fresh final M5B snapshots passed. [Final evidence](M5B.md) distinguishes these completed gates from development and the earlier deliberately interrupted capture.
