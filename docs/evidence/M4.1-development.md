# M4.1 chronological development evidence

2026-09-19 UTC. The external review supplied a code-derived/offline arithmetic counterexample, not a PostgreSQL/RabbitMQ reproduction. The unchanged local baseline at 77a13205a99f23cd73e353b87bed007f0a36cc75 passed npm ci and make verify-m4, including 53 unit checks, all 13 prior profiles and resource cleanup. Hosted M4 run 35458462908 subsequently completed successfully at that same documentation HEAD; its old inventory does not include the new boundary.

Scope/inventory were committed first in 333c7ab. The private reproduction and boundary harness were committed in efc1514. Initial static checks caught test typing/lint issues, which were corrected before execution. The first real reproduction invocation exited 1 before test launch: the existing ES project-name guard did not allow the new m41-repro prefix. Required B01 was NOT RUN and PostgreSQL cleanup passed. That failed run remains at artifacts/m41-repro/m41-repro-20260919181725179-e18bfbe8. Commit 8b231a2 explicitly permits isolated M4.1 service names and qualifies the independent SQL oracle joins; it changes no production consumer or accounting behavior.

The second invocation, `npm run test:reproduction:m41`, exited 0 as a diagnostic reproducer on clean committed 8b231a2. Actual application processing failed as predicted. Thirty-two distinct v2 revisions came from real source commands, capture and immutable pipeline exports; each wire was measured at 32768 bytes, body 32675. Total admitted wire bytes: 1048576; migration 001 SQL charge: 1049056. A restricted consumer_runtime READ COMMITTED session (backend 871, xid 858, snapshot 858:858:) raised P6002 at the process_batch byte guard and explicitly rolled back. Independent exact consumer table snapshots were unchanged.

The actual production publisher then confirmed those same 32 ledger wires. Private test timing held the return of the real consume registration until 32 actual callbacks had queued, leaving production selection/validation unchanged. Two separate real production Consumer processes (2268797 and 2268872) each submitted all 32 unique events, received P6002/rolled_back, exited 1, emitted no ordinary success and made zero ACK calls. After each channel closed the broker independently reported 32 ready and zero unacknowledged; the second attempt observed all messages redelivered. Existing v1 effects stayed exact; no v2 inbox/effect/projection increment or quarantine appeared. This is recoverable rejection/requeue, not observed message loss or an infinite observed loop. The profile's PASS means its expected diagnostic assertions passed, not successful v2 processing.

Compact machine evidence is [M4.1-reproduction.json](M4.1-reproduction.json). Full sanitized local records are under artifacts/m41-repro/m41-repro-20260919181825645-16045bf1, including actual message/channel identities, SQLSTATE, source content, queue state, retained restart and cleanup. Production correction had not yet been made when this evidence was committed.
