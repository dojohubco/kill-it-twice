# M3.1 development observations — 2026-09-19

Scope commit `1680ef3` preceded corrections. Historical reproduction code was committed as `94c0d74`, then `npm run test:reproduction:m3-oracle` exited 0 on a clean tree, run `m3-repro-20260919120649054-a34d3231`. This PASS means the intended old-oracle failure was actually observed, not corrected acceptance. [Compact reproduction](M3.1-reproduction.json) links the exact code/content hashes and local evidence.

Real source commands created valid v1 and updated the same entity to invalid v2. Ordinary capture and the production ES adapter produced a real item 400/document_parsing_exception for loyalty_points. Independent complete realtime GET before/after was identical at v1, with a retained actual dead letter for v2. Refreshed search still contained the entity. The unchanged historical oracle's final identity-set assertion failed because it excluded that entity. The reproduction asserts that exact missing expectation, not just any thrown error. Source history was retained; no production data-loss or delivery semantic defect was observed. Both database and Elasticsearch owned-resource cleanup passed.

The original oracle function is preserved under `tests/support/es-historical-oracle.ts` solely for this isolated profile. Healthy acceptance will use the corrected verifier-owned expectation model, never the known flawed historical function. Original M3 schedules and local results remain historical evidence; they did not exercise this counterexample.

Corrected oracle commit `d1b3baa` passed `npm run test:integration:m31` at `artifacts/m3-oracle/m3-oracle-20260919121643399-16f6e04f/` (exit 0; O01–O06 all passed, no skipped/cancelled/todo; database and receiver cleanup PASS). Real v1/v2 rejection retained v1; v3 corrected normally; invalid initial state stayed absent; rejected restore retained the tombstone; actual receiver deletion/content corruption were detected and restored only in the owned privileged negative-control fixture. Pure fabricated-status controls were never written into pipeline tables and are not called receiver rejections.

Static development checks caught an unused import, TypeScript's empty-array assertion narrowing of a subsequently mutated diagnostics array, and an unused exported prerequisite constant. These were corrected without suppressions or weakening assertions; the corresponding type/lint/Knip checks then passed. The prerequisite/phase unit suite passed all 49 total unit checks. It injects explicitly synthetic below/at/above values into read-only test dependencies; no workstation sysctl was changed and those values do not describe the historical hosted kernel.
