# Capacity continuation: development observations

2026-09-21. These observations are not completed scale acceptance.

The first measured change (`42149b5`) batched exact consumer receipt observations and added indexed source-history predicates. The 8,192-row fixed-window pilot still ended MEASURED_PARTIAL; receipt observations increased, but total staging did not improve relative to the earlier loaded-host sample. No universal speedup is claimed from those two observations.

The first populated source migration check failed its function ACL equality assertion. Data, function OIDs, owner and fixed search path matched exactly, but the earlier `require_current_baseline` trigger function had an implicit PUBLIC execute grant and the forward migration explicitly revoked it. That is a deliberate restriction, not an expanded permission or data change. The corrected preservation check must name that exact narrowing while requiring every other property to remain equal; the original failed report remains preserved. Its later negative SQL cases had not executed before failure.

One quality run failed the pre-existing one-millisecond observation test: the deadline expired before its first observation, producing `last observation: undefined` instead of the test's expected string. Production waiting code and that test were unchanged. The failure remains recorded in `artifacts/capacity/continuation/p2-quality.log`; a later retry is separate evidence, not a relabeling of this run.

The next change applies chunk-local deferred proof and an explicitly admission-only scanner snapshot. It retains full phase/final completion checks and historical profile defaults. New source migrations are additionally selected by `node scripts/m1.ts m5a --capacity`, which must execute the same eighteen real baseline/activation/receiver cases rather than substitute a reduced test list. Its manifest explicitly records the new source migration selection.

The first bounded sink proof (`0340ed2`, `kit-final-20260922094115-546b5486`) failed before receiver operations with EACCES: its read-only administrator fixture attempted to traverse the ES-private directory owned by uid 1000 with mode 0700 while its process had uid 0 and no capabilities. A metadata-only read confirmed that boundary. The existing setup copy of the same public certificate is already available at `/private/server.crt`; the test now reads that copy just like the existing inspector. No directory permission, capability, TLS check, receiver credential or production role was relaxed. The failed run and successful owned cleanup remain preserved.

A new coordinated unit schedule at `7e80f5d` found that a later ES claim could expire while an earlier full bulk was awaiting its result. The loop then attempted an empty prefetch, causing an unnecessary transient-admission outcome. The fixture records `[16, 0]` read sizes; it is mock scheduling evidence, not a real receiver failure. A second ownership check after the awaited flush avoids that empty read without reviving ownership or changing prior committed outcomes. The failing fixture/log is retained under `artifacts/capacity/continuation-20260922/`.

The clean 32,768-row pilot at `7e80f5d` ended FAIL with owned cleanup PASS. Its status-sampling code dereferenced a valid unavailable section (`data: null`), raising TypeError after the last saved sample at 24,768 staged events. The failing response itself was not retained, so its underlying receiver/database reason is not invented. The next measurement must record unavailable observations before reading counters and continue only inside its declared window, never turn unknown into zero or convergence. Prior 8,192 exact reconciliation and the complete functional G1-G5 run at this code did pass.

The initial parallel-pilot invocation at `b470841` exited during argument parsing because the new scanner option had not been registered; no service was started. The parser and recorded replica count are now explicit, with a pure help/invalid-value regression proving rejection occurs before resource creation. This is a measurement-driver correction, not a replication failure.

The first indexed 131,072-row attempt at abd91f5 retained unavailable status observations while real source/capture/receiver work progressed. Actual PostgreSQL read errors showed dynamic shared-memory resize failure in the pipeline's default 64-MiB `/dev/shm` (total container memory remained 1 GiB). A read-only comparison with parallelism disabled avoided that allocation but took over ten seconds; JIT disabling did not provide a viable remedy and was not applied. The agent interrupted only the identified capacity pilot through normal cleanup after recording the errors, actual container limits and last observations; its status remains FAIL and no final reconciliation or post-refresh storage was claimed. The supplementary watcher explicitly failed because receiver export never ran. The next resource change provisions only the pipeline shared-memory filesystem at 256 MiB, preserving the 1-GiB memory ceiling, all timeouts and every data/evidence assertion.

The first new static resource-budget check rejected Compose's actual JSON representation because it expected numbers; pinned Compose emits decimal strings for `mem_limit` and `shm_size`. The check now compares those exact documented byte-value strings, while runtime Docker inspection independently checks numeric HostConfig values. No resource ceiling or test condition was weakened. The original nonzero quality log is retained.

The explicit page-size continuation (`ff7e115`) first reached G1-G4 but its G5 browser screenshot failed with `Page.captureScreenshot: Unable to capture screenshot`. The underlying compositor cause was not established. The owned project cleaned up and that run remains FAIL. A read-only check of the unchanged retained demo passed, followed by an unchanged-code fresh G1-G5 gate and exact oracle PASS; no screenshot assertion or UI security policy was relaxed. The earlier attempted fixture combinations too small to contain the required retained-page/healthy/fault schedule now fail before any provisioning, with unit coverage.

A read-only due-selector plan during the fixed 131,072/page64 run showed a repeated sort/scan despite existing partial indexes. The candidate's explicit logically redundant state predicate used the existing index; a fixed-snapshot ordered membership comparison matched. Scope documentation preceded the forward change. Actual old/new rollback claim calls, immutable data/function metadata, a 54-case SQL truth table, restricted DDL denial, real receiver reconciliation and post-change G1-G5 all passed. Measured query times and whole-workload results are kept separate in `Capacity-page-and-queue.md`.
