# M5A development observations

Chronological observations, separate from final acceptance. The external review supplied the planned baseline/no-op integration requirement; it did not execute these services.

- Baseline: unchanged executable `make verify-m4-1` reached fifteen finalized passing profiles before outer execution ended 143 during final-profile finalization. The remaining test report had 8 passes, but no final restart/cleanup result; only identified resources were manually removed. Fresh standalone M4.1 upgrade repeated successfully (exit 0). See milestone and ignored baseline logs for exact identities.
- Hosted M4.1 run 35467226835 was actually FAILURE: the command wrapper selected its 600000 ms fallback for verify-m4-1. The M5A extension adds that gate and verify-m5a to an explicit longer-deadline selection, with a unit regression. No host setting or remote workflow was changed.
- Draft static checks found unused test exports/imports and boundary typing errors. These were corrected without suppressions or new dependencies. Initial applied `make quality` passed 54 unit checks.
- First real fresh profile at c408069, run m5a-20260919212024696-2dd5ef2a, exited 1: 5 passed, 13 failed, zero skip/todo/cancel. Migration and seed contention/pre/post-COMMIT fault controls ran. The independent member query accidentally ordered the text-cast ordinal alias (1,10,11...) instead of the underlying BIGINT, failing the recipe assertion before sealing; later activation-dependent tests consequently failed closed. The actual reader login also rejected UPDATE with read-only SQLSTATE 25006 before the test's expected ACL error. Corrections qualify the native ordinal ordering and separately test both the reader default and its underlying ACL using only a session-local read-write probe. Assertions still require exact recipe membership and denied writes. Cleanup passed with no secondary errors. This was a test-harness correction, not a demonstrated baseline data-loss failure.

All full run logs, input hashes, SQL/process evidence and failed reports remain in ignored artifacts and the local review bundle. Final results will be recorded separately.

Second fresh profile at 22ecb33, run m5a-20260919212355442-1469e6cf, exited 1 with 17 passes and one failure, zero skip/todo/cancel. Real activation controls/deaths, historical no-op receipt, source-derived baseline delivery through both sinks and reconciliation ran. BS01's newly created RabbitMQ queue had no management `messages` statistic yet, so the assertion rejected undefined rather than inventing zero. A bounded passive AMQP queue declaration now observes actual ready-message/consumer counts. No purge, selection or delivery behavior changes. Cleanup passed. Additional final-oracle assertions compare projected canonical bytes, explicit seed progress and historical receipt evidence, with negative fixtures; these strengthen the required checks rather than repairing source history.

At 5b056c7, fresh run m5a-20260919212943230-6e574b9f passed 18 cases and populated upgrade m5a-20260919213126859-db78e8c8 passed BS13; quality passed 54 units. Final audit then found that BS14 relied on orchestration's PostgreSQL restarts but did not itself restart the two receiver services for this new baseline fixture. The first complete capture attempt was deliberately stopped after its eight earlier profiles finished and cleaned up, before ES/broker profiles started. Its FAIL/incomplete result and controlled-stop diagnostics are retained; that orchestration termination is not a required application fault or a full gate pass. BS14 now explicitly restarts the owned ES and RabbitMQ containers, validates the same registrations, and compares actual receiver documents and exact source state again. PostgreSQL retained restart remains separately required by orchestration. Two final complete gates will use the resulting committed code.

The first complete `make verify-m5a` at f807e2b passed (exit 0, 18 profiles, 54 unit checks). Its fresh bootstrap run m5a-20260919222017343-4140a76b passed 18 cases and its populated upgrade passed BS13. The required second invocation then failed IC06 in the historical M2C profile, run m2c-20260919222721896-9e1169e2: 18 passed, one failed, zero skip/todo/cancel, cleanup passed. The assertion saw no higher claim generation after worker B returned. That old test emitted detailed reclaim evidence only after the assertion, so the failed record cannot establish the exact scheduling cause. It remains a failed/incomplete repeat, not a full acceptance pass.

Code inspection found a concrete test coordination gap: SIGSTOP at the post-pipeline barrier could freeze an independent renewal transaction while it still held the work-row lock. An observer can see the previously committed expired lease while another claimer correctly skips that locked row. IC06's private instrumentation now stops admitting renewal SQL and awaits existing renewal-owner completion before announcing this particular barrier; an independent source-session query must confirm quiescence. Actual SIGSTOP/SIGCONT, database-clock expiry, higher-generation reclaim, immutable stage/ACK and stale-result assertions remain. A labelled real SQL row-lock control additionally requires an otherwise expired row to be skipped until its lock is released. Reclaim diagnostics are written before assertions. Production capture/SQL behavior and required inventories are unchanged. The raw failed profile and the distinction between observed failure and inferred scheduling cause are retained under development artifacts. Two final complete gates will be repeated on the resulting committed test code.

The targeted real M2C repeat at fd69bed, m2c-20260919223329917-59975d2b, passed all 19 cases (exit 0, cleanup PASS). Its labelled lock control observed source time 22:33:49.768623+00 later than lease 22:33:49.752408+00, while the competing worker correctly reported zero claims under the held row lock. After release, generation 1 became acknowledged generation 2; resumed A could not alter it. This proves the lock-control mechanism, not the unrecorded exact cause of the earlier failure. A final small test-only refinement confines the pause-abort check to the explicit IC06 renewal gate, preserving other fault workers' prior stop behavior. Fresh complete acceptance will use that committed state.
