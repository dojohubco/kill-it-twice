# Consumer redelivery observation, 2026-09-25

Clean `91d606b29426c100fe58b6909bcc6526db05880a` failed the 1024 functional run at G2. G1 passed; G3–G5 and both reconciliation stages did not run. The sequential supervisor prevented the full-million run from starting. Cleanup passed, all 27 preexisting containers remained, and the failed clone and evidence are retained.

The consumer was killed after its original database commit and before broker ACK. Those durable counters already satisfied `settled()` after restart, so the verifier read the trace immediately. The event snapshot finished at 10:24:17.844494 UTC and failure diagnostics began at 10:24:17.854228. The restarted worker logged completed redelivery at 10:24:17.908762. Its retained trace has two deliveries of the target with identical wire hashes, the broker redelivery flag, and `processed` then `already_processed` commit outcomes. The before/after inbox (including xmin) and effect were identical. [Selected failure evidence](Consumer-redelivery-wait-2026-09-25.json) retains these facts; the failure remains FAIL.

Review of the exact command sequence identifies the immediate consumer trace assertion as the failing check: the preceding durable comparison passes on its captured snapshots and no publisher-control command began. A settled durable total proves the original commit; it cannot prove that a newly restarted process has received and committed its replay.

The correction adds a bounded observation of the actual target replay before taking the after snapshot. It requires at least two target deliveries, a broker redelivery flag, identical wire hashes and an `already_processed` commit for that target. The original exact inbox/effect comparison and delivery assertions remain. Missing evidence still fails. The new observation uses the existing G2 consumer-evidence allowance of 15 seconds; settle, fault barrier, query, phase and enclosing deadlines are unchanged. No application or transport behavior changes.

Focused regression covers delayed delivery followed by a later duplicate commit, missing replay, delivery without duplicate commit, another event's replay, changed bytes and a missing broker redelivery flag. It uses deterministic time only for the local helper regression; required acceptance continues to use real RabbitMQ delivery and SIGKILL. `python3 -B tests/final/runtime_test.py` passed all 12 ownership and evidence tests. New clean functional and million-entity runs remain required before publication.
