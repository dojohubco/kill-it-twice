# Final acceptance matrix

Status: **NOT READY**. The latest million-row attempt at `6662839` FAILED during drain because private fault mocks retained call history until the worker heap was exhausted. The corrected `79ad592` passed fresh small faults, but its default command failed the separate R07 browser check before large execution. The corrected candidate awaits full execution. States: IMPLEMENTED means a code path exists; VERIFIED requires inspected matching execution evidence; UNVERIFIED means required current evidence is absent; FAILED means an attempted mandatory check failed. Historical passes have their own SHA and are not substituted for current verification.

| Original criterion                                                                   | Implementation      | Current acceptance state                            | Entry point / executable command                                                   | Evidence                                                                                            |
| ------------------------------------------------------------------------------------ | ------------------- | --------------------------------------------------- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Relational source, concurrent backfill and incremental capture                       | IMPLEMENTED         | UNVERIFIED at final dataset                         | source triggers, `src/backfill/`, `scripts/verify-final.py`; `make verify` G1      | Inherited 262144 reconciliation at `93eac82`; final run pending                                     |
| Search receiver and event stream with independent consumer                           | IMPLEMENTED         | UNVERIFIED at final dataset                         | `src/es/`, `src/rabbitmq/`; `make verify` G2-G4 + oracle                           | [Inherited results](evidence/Observation-terminal.md)                                               |
| Real G1-G5 through single `make verify`                                              | IMPLEMENTED         | UNVERIFIED                                          | `scripts/verify-submission.py`, `scripts/verify-final.py`; `make verify`           | New executable replaces the historical placeholder; pending                                         |
| UI status and useful metrics                                                         | IMPLEMENTED         | UNVERIFIED at final code                            | `apps/operator-ui/`, `scripts/runtime-browser.ts`; G5                              | Existing small results; fresh checks pending                                                        |
| UI search/details, controls/replay/configuration, failure/corrupt/source simulations | IMPLEMENTED         | UNVERIFIED at final code                            | `scripts/runtime-operator-browser.ts`; `make verify-runtime` R05/R07               | Real control flow added; execution pending                                                          |
| Root Compose, explicit seed and retained restart                                     | IMPLEMENTED         | UNVERIFIED at final code                            | `compose.yaml`; `docker compose up -d --build`, `make seed`, `make verify-runtime` | Fresh isolated installation pending                                                                 |
| Nontrivial measured dataset and bounded worker memory                                | IMPLEMENTED         | UNVERIFIED                                          | `make verify` default 1M, `scripts/capacity/resources.py`, `storage.ts`            | Payload, CPU/RSS/cgroup/storage and exact comparisons pending                                       |
| Honest delivery, DLQ and supported replay                                            | IMPLEMENTED         | UNVERIFIED at final code                            | ES current-attempt replay, consumer quarantine inspection; G2/G4/R07               | README and ADRs 002/010/011/014; execution pending                                                  |
| SPEC before code, chronological evolution, AGENTS                                    | IMPLEMENTED         | VERIFIED in local Git                               | `git log --reverse`, `git show 251cb5a`                                            | SPEC first `251cb5a`, harness `7af0818`, implementation `5368de6`; no history rewrite               |
| README diagram, >=4 ADRs, capacity/exclusions, >=2 actual AI deviations              | IMPLEMENTED         | VERIFIED documentation audit; final results pending | README; ADRs 001-015; capacity notes                                               | Consumer wire accounting `0a375fd` / B02-B08; oracle `d1b3baa` / O01-O06, linked real reproductions |
| Public repository submission handoff                                                 | IMPLEMENTED locally | UNVERIFIED publication, separately authorized       | Local commit/evidence handoff; no push or upload                                   | No publication authorized; local versus remote commits to be enumerated                             |

The failed full attempt, confirmed 12-second SQL cancellation, unchanged 18.078-second proof, and scoped correction are retained in [closure development evidence](evidence/Acceptance-closure-development.md). The failed attempt is not replaced by the passing 1024-row repeat.

## Candidate and evidence identity

Initial reviewed code and completed inherited sequence: `93eac82044068e697c42545fb4f71041f8e8baef`. Closure note `0be5d2a` precedes implementation. Final tested code, any later documentation SHA, exact commands/exit codes and sanitized bundle will be added only after execution. Historical failures remain retained.

## Repository hygiene scope

Read-only local reachable-history scan inspected 1079 Git blobs against four signature families: PEM/OpenSSH private keys, GitHub tokens, AWS access-key identifiers and OpenAI keys. No matches found. Tracked filename scan found no resume/interview/application/private-key/.env candidates. This is not exhaustive secret detection; generated installation credentials and private volumes are excluded from publication. No separate original-assignment file was found among tracked filenames; the retained pre-code SPEC and current user acceptance requirements provide the mapping above.
